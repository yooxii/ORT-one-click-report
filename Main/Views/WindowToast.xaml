<Window x:Class="ORT一键报告.Main.Views.WindowToast"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False" ShowActivated="False" Topmost="True"
        SizeToContent="WidthAndHeight" WindowStartupLocation="Manual"
        Focusable="False" IsHitTestVisible="False">
    <!-- Toast 提示窗口：无边框透明、不抢焦点、置顶；位置由 ToastService 按设置计算（默认聚焦窗口右上角） -->
    <Border x:Name="rootBorder" Background="{DynamicResource CardBackgroundBrush}"
            BorderBrush="{DynamicResource BorderSubtleBrush}"
            BorderThickness="1" CornerRadius="{DynamicResource CornerRadiusCard}"
            Padding="14,10" Margin="10">
        <StackPanel Orientation="Horizontal" MaxWidth="460">
            <Ellipse x:Name="dot" Width="10" Height="10" Margin="0,0,10,0" VerticalAlignment="Center"/>
            <TextBlock x:Name="txt_msg" Foreground="{DynamicResource TextPrimaryBrush}"
                       VerticalAlignment="Center" TextWrapping="Wrap" MaxWidth="420"/>
        </StackPanel>
    </Border>
</Window>
