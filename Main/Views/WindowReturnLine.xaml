<Window x:Class="ORT一键报告.WindowReturnLine" Background="{DynamicResource WindowBackgroundBrush}" Foreground="{DynamicResource TextPrimaryBrush}"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        xmlns:local="clr-namespace:ORT一键报告.Views"
        xmlns:viewmodels="clr-namespace:ORT一键报告.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewmodels:ReturnLineViewModel}"
        mc:Ignorable="d"
        Title="{lex:Loc ReturnLine_Title}" Height="450" Width="800">
    <Window.Resources>
        
    </Window.Resources>
    <Grid Margin="25">
        <Border BorderBrush="{DynamicResource BorderSubtleBrush}" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="60"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="{lex:Loc ReturnLine_FormPath}" VerticalAlignment="Center"/>
                    <TextBox x:Name="textbox_lt" TextWrapping="Wrap" Text="{Binding LTPath}" Grid.Column="1" Margin="3"/>
                    <Button x:Name="btn_lt" Grid.Column="2" Margin="3" Content="{lex:Loc Common_Browse}" Command="{Binding SelectLTPathCommand}"/>
                </Grid>
                <Expander Header="{lex:Loc Common_Filter}" Grid.Row="1" IsExpanded="True">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="60"/>
                        </Grid.ColumnDefinitions>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Content="{lex:Loc ReturnLine_RequisitionDate}" VerticalContentAlignment="Center"/>
                            <DatePicker Grid.Column="1" Margin="3" SelectedDate="{Binding StartDate, UpdateSourceTrigger=PropertyChanged}"/>
                            <Label Content="~" Grid.Column="2" VerticalContentAlignment="Center"/>
                            <DatePicker Grid.Column="3" Margin="3" SelectedDate="{Binding EndDate, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>
                        <Grid Grid.Column="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Content="{lex:Loc ReturnLine_RTOrder}" VerticalContentAlignment="Center"/>
                            <TextBox Grid.Column="1" Margin="3" Text="{Binding StartRTAH, UpdateSourceTrigger=PropertyChanged}"/>
                            <Label Content="~" Grid.Column="2" VerticalContentAlignment="Center"/>
                            <TextBox Grid.Column="3" Margin="3" Text="{Binding EndRTAH, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>
                        <Button Grid.Column="2" Margin="3" Content="{lex:Loc Common_OK}"/>
                    </Grid>
                </Expander>
                <GroupBox Header="{lex:Loc Common_Preview}" Grid.Row="2">
                    <StackPanel>
                        <TextBlock Text="{lex:Loc ReturnLine_CheckHint}" HorizontalAlignment="Center" Visibility="Collapsed"/>
                        <local:UserControlReturnLineSingle Visibility="Visible"/>
                    </StackPanel>
                </GroupBox>
            </Grid>
        </Border>
    </Grid>
</Window>
