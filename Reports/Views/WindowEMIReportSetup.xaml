<Window x:Class="ORT一键报告.Reports.Views.EMIReportSetup" Background="{DynamicResource WindowBackgroundBrush}" Foreground="{DynamicResource TextPrimaryBrush}"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        xmlns:local="clr-namespace:ORT一键报告"
        xmlns:viewmodels="clr-namespace:ORT一键报告.Reports.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewmodels:EMISetupViewModel}"
        mc:Ignorable="d"
        Title="EMIReportSetup" Height="450" Width="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Menu Margin="3,0,0,3">
            <MenuItem Header="{lex:Loc EMI_Reload}" Command="{Binding LoadFromExcelCommand}"/>
            <MenuItem Header="{lex:Loc EMI_SaveSettings}" Command="{Binding SaveSettingsCommand}"/>
        </Menu>
        <local:BaseSettings Grid.Row="1"/>
    </Grid>
</Window>
