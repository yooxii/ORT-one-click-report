# ORT实验室管理系统修改记录

> 精简版：按日期保留变更要点，原详细文件清单、现象/根因与逐项验证记录已压缩。完整历史可查看 Git 中的旧版本。

## 2026-09-21

- **修复计划表新增/导入后 RT 工作编号生成不正确**（`Services/PlanExcelService.cs`）：
  - 原逻辑用 `COUNT+1` 且只按单一前缀过滤（RT 或 QRT），导致两种情况会算错：① 同月已有 RT260921、QRT260922 时新增 RT 只数到 1 条 RT 记录→ 生成 RT260922（与已有 QRT260922 同序号）；② 删除历史记录后 COUNT 变小→ 产生重号。
  - `GenerateJobNo` 改为：RT 与 QRT 共享同一个月度序号，同时查两种前缀的记录，用严格正则 `^(?:QRT|RT){yyMM}(\d+)$` 提取末尾序号取最大值，下一笔 = MAX+1；序号 ≤ 99 时补到两位（保持“最后两位 = 当月第几笔”的读法），≥ 100 时按实际位数展开避免被截断后重号。前缀参数自动大写，空值默认 RT。
  - `GenerateReturnRtOrder`（RTAH 回线RT工令）同样从 COUNT+1 改为 MAX+1，避免删除后重号。
  - 新增两个私有工具方法：`MaxTrailingSequence`（按正则从一组编号里取末尾序号最大值）、`FormatSequence`（序号格式化）。
  - 导入路径（`ImportSchedule`）按 Excel 里的工作编号原样写入，不自动生成；修复后的生成器查询 DB 时已包含导入记录，因此导入后手工新增计划能正确拿到 MAX+1（无需修改导入逻辑）。
- **程序名称变更**：产品名称由「ORT一键报告」改为「ORT实验室管理系统」（英文：ORT Lab Management System，繁中：ORT實驗室管理系統）。同步更新 `AssemblyTitle`/`AssemblyProduct`、四个资源文件里的 `App_MainTitle`/`App_ReportTitle`、`MainReportViewModel` 中拼接报告窗口标题的硬编码字符串（改为取 `App_ReportTitle`，避免后续再改产品名时漏改），以及 `App.OnStartup` 里的启动日志文本。项目名、根命名空间与程序集名（`ORT一键报告`）保留不变，避免大范围重构；仅面向用户可见的产品名称统一改名。
- **数据文件夹启动失败回退到默认目录**：`DatabaseService` 构造时先按本机设置里的 `DataFolder` 初始化，若失败（UNC 映射不成功、目录不可写、SQLite 打不开等 `DataFolderUnavailableException` 场景）且配置路径不等于程序目录下的默认 `Data`，自动用默认 `Data` 重试一次；回退只对本次运行生效，不修改 `local_settings.json`（避免网络共享临时不可达就把用户配置清掉）。启动成功后 `App.OnStartup` 会弹一次告警窗口告知回退情况与失败原因，同时写 Warn 日志；如果连默认目录也起不来，仍抛原来的 `DataFolderUnavailableException`（保留原有致命错误提示与 `Shutdown(1)` 行为）。四个资源文件新增 `Db_FallbackToDefaultTitle` / `Db_FallbackToDefaultFormat` 两个本地化键。
- **新增/编辑窗口改为非模态**：`WindowRequisitionEdit`（领退新增/编辑）与 `WindowPlanDirectEdit`（计划新增/编辑）从 `ShowDialog()` 改为 `Show()`，去掉 `DialogResult`，改为触发 `Saved` 事件后 `Close()`；`PlansViewModel` 的 `AddRequisition`/`AddPlan`/`EditRequisition`/`EditPlan` 订阅 `Saved` 回调处理暂存/提审。窗口打开期间主界面与表格仍可点击，可同时开多个新增/编辑窗口（各自持有自己的编辑目标）。
- **计划表新增「报告状态」列**（`Plan.ReportStatus`，新增 `Models/ReportStatusKind.cs`）：可选值 已完成 / 进行中 / 无要求。新增 `Services/ReportStatusReader.cs` 读报告概览的 `TestStatus` 表：在前 10 行定位含 `TEST ITEMS`/`UNITS UNDER TEST`/`TOTAL`/`FAIL` 的表头行，逐个测试项行判定 `UNITS UNDER TEST == TOTAL + FAIL`；全部满足→已完成，否则→进行中（含全 0 未开始）；找不到表/表头/测试项行→返回 null 不改动。自动优先，用户手工设为「无要求」后扫描不再覆盖（`ReportStatusKind.IsUserLocked`），改回其他值则下次扫描重新接管。计划表 DataGrid 与两个编辑窗口都加了该列/下拉。
- **报告扫描重构为可中断的高耗时任务**：新增 `Services/ReportScanService.cs`（从 `PlansViewModel` 迁出扫描核心逻辑，支持 `CancellationToken`，每处理完一个报告夹检查取消并读 TestStatus 写回报告状态）、`Services/ReportScanScheduler.cs`（仿 `PlanIndexScheduler`，闲置 N 秒后自动触发，默认 300 秒，`report.scan.auto`/`report.scan.idleSeconds` 两个设置项）、`Services/HighCostTaskCoordinator.cs`（一键报告/计划索引/报告扫描三者互斥：启动新任务时先 Cancel 旧任务→弹「正在中断」等待窗→等旧任务收尾→再启动）、`Main/Views/WindowInterruptWait.xaml`（非阻塞等待窗）。扫描进度条挂在 `WindowPlans` 右下角状态栏（含「停止扫描」按钮）。**移除**了打开领退与计划窗口时、以及刷新时的自动扫描（`Refresh()` 不再连带扫描）；扫描改为手动菜单/工具栏触发或闲置自动触发。`WindowPlans` 关闭时退订对单例扫描服务的订阅（`DetachScanEvents`），避免 Transient ViewModel 被 Singleton 长期引用。一键报告（`WindowMainReport.DoReport_Click`）与计划索引（`WindowPlans.Menu_PlanIndex_Click`）都改为走协调器启动。
- **主窗口「用户」入口下移 + 登录前置 + 游客登录**：主窗口菜单栏去掉「用户」菜单，改为在「审核」按钮下方新增「用户」按钮，点击弹出 `ContextMenu`（用户中心 / 登录-注销），未登录时「用户中心」灰化。`App.xaml` 去掉 `StartupUri`，`App.OnStartup` 里在创建 `MainWindow` 之前先弹 `WindowLogin`：登录成功或点「游客登录」→打开主界面；点「退出」（原「取消」）→`Shutdown(0)`。`WindowLogin` 新增「游客登录」按钮（`IsGuestMode=true`，以 `CurrentUser=null` 进入）与「退出」按钮（用 `IsGuestMode` 区分游客进入与退出程序）。设置界面新增「报告扫描」节（仅管理员可见）。四个资源文件新增 `Plans_ReportStatus`/`ReportScan_*`/`Interrupt_*`/`MainWindow_Button_User`/`Login_GuestButton`/`Login_ExitButton` 等本地化键。

## 2026-08-06

- **修复**：读取报告时模板路径为空不再抛异常，改为跳过表头读取并记录 Warn；修复日志窗口打开时空引用、`LevelCellStyle` TargetType 不匹配导致的崩溃。
- **新增**：日志查看窗口，支持按日期/级别/关键字筛选、排序、自动刷新、异常堆栈并入上一条；主窗口与报告窗口菜单增加入口。
- **重构**：清理空实现；拆分 `Utils/Report.cs`；消除 `Docx2Pdf` 重复代码；完善依赖注入容器；解耦 View-ViewModel 静态依赖；把视图业务逻辑迁移到 ViewModel。

## 2026-08-18

- **新增计划管理模块（Plans）**：SQLite 全局数据库、plans 表、计划/领用表导入导出、OLE 附件提取、权限预留、管理界面与 DI 注册。
- **修复**：导入时 EPPlus OLE 提取抛空引用导致调试器中断，改为优先 zip 直读 embeddings，失败再回退 EPPlus。
- **合并/筛选优化**：改为「计划表基底 + 领用表辅助」单行合并模型；支持数据清空、多维动态筛选、搜索防抖、列拖动与布局持久化、SN 文件点击打开。
- **关联修正**：工令/日期+机种关联规则调整，计划表与领退记录合并率大幅提升。
- **权限与审核**：新增用户/角色/登录/注销、审核工作流、管理模块（人员/客户/测试项目）；回线转移单入口迁移；一键报告按 RT 工号/机种匹配计划数据。
- 所有新窗口与服务完成 DI 注册，MSBuild 编译通过。

## 2026-08-19

- **Excel 风格编辑**：测试项目以计划表为准；新增行/单元格编辑先暂存，手动提交统一写库并记录变更日志；右键菜单、编辑校验、字典约束、机种/测试项目联动。
- **修复**：单元格编辑提交时 `InvalidOperationException`（EditItem 事务中 Refresh），实体改为带属性通知的 `ObservableObject`，移除编辑路径中的 Refresh。
- **界面/数据改进**：日期列改 `DateTime?`、单元格垂直居中、列可隐藏/布局持久化、移除界面冗余列；修复主窗口无法置前；客户-产品别整合后按需求撤销并恢复分表。
- **修复**：PowerShell 批量改文件导致编码损坏，使用 `git restore` 恢复后重新应用修改。
- **分表重构**：计划表与领退表拆分为独立实体/表/双 Tab 界面，分别导入导出，新增自动编号、领退/计划新增编辑表单，提审流程支持领退表。
- **浏览编辑分离**：日期与下拉列浏览/编辑分离；表格内测试项目联动；右键单元格直接编辑；清空数据入口迁移到管理模块。
- 日期解析支持「2026/8/18」「8月7日」等格式。

## 2026-08-20

- **修复**：工程文件重复引用；编辑对话框直接写库改为走暂存/提审；机种改名自动带出产品别/客户别；自动补全字段允许手改。
- **代码映射**：新增 `code_mappings` 表，支持同一产品/客户名称对应多个代码；客户别/产品别按机种名代码规则查询；EPPlus 许可统一到程序入口。
- **界面调整**：管理窗口改名与列顺序优化；表格单元格样式/居中/右键菜单完善。

## 2026-08-26

- **修复**：选中单元格文字变白；领退表单 SN 栏支持回车换行与高度自适应；编辑领退记录时自动带入对应计划记录。
- **新增设置页面**：VSCode 风格左树+右详情；JSON 保存 UI 字体/字号与常用目录，支持实时应用；主窗口新增设置入口。

## 2026-08-27

- **设置入库**：设置改为保存到数据库 `app_settings`；数据库路径作为独立本地配置；ATE/EMI 路径也改为程序目录本地文件保存。
- **设置交互**：设置窗口改为保存/取消/应用按钮；数据库路径仅管理员可修改。
- **报告对应**：新增报告夹扫描 `report_links`；计划表右键可打开报告文件夹/概览；计划表按是否有报告区分颜色、行列选中高亮。
- **体验/提速**：引入 Ookii.Dialogs.Wpf 原生文件夹选择；报告扫描异步化并持久化到数据库，大幅提升领退和计划窗口打开速度。

## 2026-08-28

- **窗口行为**：主窗口退出时关闭全部子窗口；领退和计划窗口单实例（已开则聚焦）；注销时自动关闭无权限子窗口。
- **登录体验**：本地 Cookie 保留上次登录，一周内免密续登；修复继续登录按钮溢出。
- **一键报告**：计划表右键打开一键报告并携带计划/领退预填数据；提取 `ReportInputModel` 模型与 `ReportGenerationService` 生成入口；报告 Tab 改为按需懒加载并持久化勾选状态；清理无效菜单。
- **扫描持久化**：报告夹扫描结果落库，打开时不再遍历文件系统，刷新时才重新扫描。

## 2026-09-03

- **多语言本地化**：引入 WPFLocalizeExtension，建立简体中文/繁体中文/English 三语资源；完成全部 XAML、MessageBox 与 code-behind 字符串本地化。
- **语言服务**：新增 `LanguageService`，支持设置中切换语言并持久化。
- **修复**：内置 Provider 对中文程序集名解析失败导致界面显示 `Key:xxx`，新增自定义 `OrtLocalizationProvider` 绕过。
- **UI 主题**：新增 Fluent/Material/DarkLab 三套主题，设置中可运行时切换并持久化。

## 2026-09-04

- **深色主题**：通过 DWM 沉浸式深色模式统一系统标题栏/窗口边框；主题化滚动条与 ToolBar，修复 ToolBar 按钮垂直堆叠。
- **自定义主题**：支持运行时导入用户自定义主题（按语义键校验、自动判定深色标题栏），无需重新编译。
- **可读性修复**：当前行/选中行底色改用语义键 `DynamicResource`，深色主题下不再出现白底浅字。
- **交互新增**：Toast 通知窗口与 `ToastService`；下拉单元格改为浏览文本、双击才出现选择框；报告路径为空时以 Toast 提醒；设置中可配置通知位置。

## 2026-09-16

- **ATE 数据工具重做**（`Reports/Views/WindowATE`，逻辑抽到 `Utils/AteReportData`）：
  - 读取：用 `ExcelNpoi.OpenAny` 直接读 `.xls/.xlsx`（不再用 Excel COM 转 xlsx）；数据取「S/N 下一行」到「MAX_SPEC 上一行」，`MAX_SPEC`/`MIN_SPEC` 两行作为上下限。
  - 分组：三栏「未使用 / 试验前 / 试验后」；试验前、试验后各最多选「原始数据条数 ÷ 2」条；行可拖动调整顺序，也可拖到另一组改归属，数值可直接双击编辑；自动分组规律不可靠时全部放进「未使用」由用户自己选。
  - 校验：超出上下限的数值标红（限值为空或 `*` 视为不限制），新增「跳到下一个超限」逐个定位并进入编辑。
  - 输出：按钮改为「保存」「提交到报告（可选报告类型）」；提交后 ATE 文件直接写入 Thermal Shock / Burn In 报告页的 ATE 数据，生成报告时作为 OLE 附件嵌入。
  - 生成：统计块（Min/Max/Average/判定）公式按实际行数重写并写入算好的结果；数值一律按数字写出（此前按文本写出会让 MIN/MAX/AVERAGE 全算成 0）。
  - 修复 `ExcelNpoi.DeleteRows` 截断合并区域时可能产生「单单元格合并区域」导致生成失败；界面字符串全部本地化（4 个资源文件 +24 键）。
  - 界面调整：「保存 / 提交到报告」移到底部，其余按钮收成一行；未使用/试验前/试验后改为垂直排列（宽度拉满，能看到更多数据）；打开数据时显示「正在打开」提示并在后台线程读取。
  - 修复：数据条数不等于 3 时，分组标签（试验前/试验后）单元格会被删掉、合并区域错位（只保存试验后时格式明显错乱）→ 标签按最终行数重写，并在写完后按最终行数整表重建标签合并区域；0~3 行 × 试验前/后各种组合逐项核对过标签、合并与统计区域。
  - 拖动时高亮「这一行会落到哪一组」；目标组已满时的提示改为说明「同一组内拖动只调整顺序，换组请先用移出腾位置」。
  - ATE 窗口不再设置 Owner：最大化后不会一直压住一键报告窗口，一键报告可正常切到前台。
  - 修复：ATE 窗口底部「报告类型」下拉在去掉 Owner 之后取不到列表（原来读的是 Owner）→ 改为直接取静态列表，不再依赖属主。
  - 字体：报告内容（表头字段与取值、测试描述、明细表内容、ATE 数据）固定为常规字重，不再跟随「设置→字体字重」整体加粗；页签、表格列头等界面元素保持原样。
  - 等待窗口重做：卡片式布局 + 图标徽标（按类型取主题语义色）+ 标题/说明 + 不确定进度条；等待窗口至少显示 0.4 秒，操作很快时也有明确反馈；ATE 生成报告（保存 / 提交到报告）同样改为等待窗口 + 后台线程，界面不再假死。
- **主界面**：四个功能按钮改为垂直排列并靠右；新增「主界面背景图」（设置→界面 里选图或清除，仅主界面生效，图片铺满窗口并加淡淡遮罩保证按钮与文字可读）。
- **报告模板工具**：「添加测试项」不再手工输入名称，改为从测试项目表（test_items_catalog）中搜索/多选添加（分类/抽样计划/测试条件/通过判定仍取计划模板，试验周期取测试项目表里维护的小时数，重复项自动跳过并提示）；「客户」改为取**计划表记录的客户别**（计划里没有时再退回机种映射）。
- **计划表右键**：该记录已有报告时，入口由「建立报告模板」变为「查看报告模板」，直接打开报告文件夹里的报告文件；文件夹里有多个报告文件时弹窗选择（新增通用单选对话框 `Main/Views/WindowListPicker`），没有报告时仍走原来的建立模板流程。
- **一键报告（从计划表进入）**：不再用计划表文案填写报告表头（测试人/审核人/项目名/阶段/描述），表头只以绑定的报告文件夹里的真实报告为准、缺失即留空，仅保留计划表能给的「测试周期」；勾选后才创建的 Tab 同样会刷新表头与单体数据。
- **修复报告明细表不显示数据**：`UserResultDataGrid.AddRow` 用属性赋值初始化 `DataGridSource` 会把 XAML 绑定顶掉，改为 `SetCurrentValue`，序列号/工令/版本/周期（周期列绑 DC）现在能正常显示。

## 2026-09-17

- **测试计划管理**（新增 `Models/TestPlan.cs`、`Services/TestPlanService.cs`、`Admin/Views/UserControlTestPlans`，管理窗口新增「测试计划」页签）：
  - 计划按「机种 + 阶段」维护，阶段只有量产（MP）与新机种（NPI，报告里的 EVT/DVT/PVT 归并为 NPI）；普通机种只需 MP 计划。
  - 每项测试含 测试项目 / 分类 / 抽样计划 / 测试条件 / 通过判定 / 备注 / 试验周期；测试项目保存时自动登记到「测试项目字典」，另给「补登记测试项目」按钮兜底。
  - 界面左侧计划列表、中间测试项明细（支持上移下移与按住行拖动排序，顺序落库）、右侧明细编辑器与「模板对照」；另有「计划项模板」页签（维护公共文本）与「待确认差异」页签。
  - 「重复内容只存一次」：公共文本存在 `plan_item_templates`，各机种计划明细只保存与模板不同的字段（`OverriddenFields` 记录差异字段），实际取值 = 差异 ?? 模板。
- **建立计划索引**（新增 `Services/PlanIndexService.cs`、`Utils/OrtPlanParser.cs`）：
  - 扫描报告根目录（文件夹含 Report 子目录 + Excel 概览）→ 逐份解析概览里 ORT Plan 表（按关键字定位 NO./TEST ITEMS/SAMPLING PLAN/TEST CONDITIOIN/PASS CRITERION/COMMENT，兼容分类行、空行、表末 Note 与列位置差异）→ 原文先进 `plan_index_raw_items` 再统一归并。
  - 归并：同一测试项目按出现次数最多的写法生成共享模板，其余按机种存为差异并列入「待确认差异」（含"其他报告的写法"参考），归并过程可重复执行；已人工确认的差异不会被重建覆盖。
  - 任务与明细落库（`plan_index_jobs` / `plan_index_entries`），每条明细带认领人与认领时间（10 分钟超时可被接管），因此可在多个客户端之间断点继续；同目录任务增量复用，新报告自动补进待处理明细。
  - 执行方式：管理窗口/领用和计划菜单主动执行（后台线程，界面不阻塞，进度条取自库里的任务记录，其他客户端在跑也能看到）；报告文件夹扫描完成后提示是否建立索引；「设置 → 计划索引」可开启空闲自动执行（管理员登录且空闲 N 分钟后自动接着跑）。
  - 归并后自动把测试项目登记进测试项目字典，试验周期按条件文本里的 168 Hrs 之类推断，取不到按测试项目名给经验值（可改）。
- **报告模板工具**（重写 `Reports/Views/WindowReportTemplate`，新增 `Services/ReportTemplateService.cs`）：
  - 以 `Templates\# ORT Test Report (WK#)_#` 为骨架，按机种测试计划直接生成报告模板文件夹（命名沿用 `{机种} ORT Test Report (WK周期)_{RT工号}`），复制模板（含 Report 子目录里的分项报告模板）后写入概览工作簿。
  - 工作簿顺序固定 `Cover | ORT Plan | Waterfall | TestStatus`；Cover 填 客户/机种/版本/产品阶段（J12/J14/J16/J18，日期类公式保持原样自动计算）；ORT Plan 表按计划生成（标题 + Created By/Model + 表头 + 分类行/测试项行 + 表末 Note）；Waterfall 填序列号、序列号下一行填工令、E4 填起始日期（自动对齐到工作日），并按每项试验周期把测试安排写进右侧日期区（同一天范围的同名测试合并单元格，每个序列号都排一遍）；TestStatus 按计划重建分类行/测试项行并重算合计（分类名按历史报告写作 ENVIRONMENT TESTS / EMC）。
  - 界面：机种计划下拉（选中即载入计划）、客户/RT工作编号/周期/版本/测试阶段/序列号/工令/开始日期/输出目录（带默认值，均可改）、测试项目安排表（可拖动排序、日期可改、可增删）、选中项的测试计划文本（抽样/条件/判定）、按周期自动排期与生成按钮。
  - 入口：领用和计划窗口的右键「建立报告模板」（仅对还没有报告文件夹的记录）、该窗口「工具」菜单与工具栏的通用入口，以及主窗口「工具」菜单。
- **其它**：`ExcelNpoi.CellStyleSpec` 增加 `Bold`；新增 `Utils/DataGridRowDrag`（DataGrid 行拖动排序）；`AppSettingsService` 增加 `GetInt/SetInt/GetText/SetText`；csproj 增加把 `Templates\**` 复制到输出目录，四个资源文件各新增 136 个本地化键。
- **验证**：用未随程序发布的控制台/界面冒烟程序（放在 `bin\Debug\_dump`，不参与提交）对 `D:\Desktop_Li\WorkDir\123` 下 15 份已完成报告跑了整条链路——解析出 5 个共享测试项模板、15 个机种计划、46 条明细、37 处差异；并用 POJ002-280G 的计划生成了报告模板，逐个核对工作表顺序、Cover 取值、ORT Plan 内容、Waterfall 序列号/工令/日期与测试安排、TestStatus 分类与行号，四个窗口（管理/报告模板工具/领用和计划/设置）均能正常打开。
- **修复：生成的报告被 Excel 提示"检测到错误…已删除记录"**：报告模板是从历史报告另存来的，里面残留 641 个命名区域（547 个 `#REF!`）、45 个外部工作簿引用（缓存值已失效）与指向已删工作表的 calcChain，且大量公式是共享公式；NPOI 原样写回后 Excel 打开就要修复（命名区域/外部公式引用/共享公式/公式(计算属性) 四类记录被删）。现在生成时先用 `ExcelNpoi.NormalizeFormulas` 把全簿共享公式就地重写成普通公式（主单元格已被覆盖的去掉公式、保留值），保存后用新增的 `Utils/XlsxCleaner` 改 xlsx 包：删掉 calcChain 与 externalLinks 部件及其关系/内容类型声明、去掉失效命名区域与 `<externalReferences>`，并打开"全量重算"。验证：新生成文件里 calcChain / externalLinks / 共享公式 / 失效命名区域均为 0，工作表、图片与超链接不受影响。
- **Waterfall 版式按历史报告还原**：日期行改为"第一列填开始日期、其余列公式顺推"，格式沿用模板的 `m/d`；星期行与 UUT 序号行按模板重写（原来把 `DAY()` 写进了本应是 UUT 序号的行）；排期结束之后的列清空，不再留着上一条报告的日期；**多个工令各占一行**（原来挤在一个单元格里换行）；测试项单元格沿用模板测试项行的格式（含最后一行下框线）。
- **TestStatus 版式按历史报告还原**：合计行/分类行/测试项行改成"逐列沿用模板对应行的样式"（分类行是主题淡蓝底、测试项名是蓝字下划线）；超链接按测试项重建，指向 `Report\` 下它自己那份报告（文件名按测试项名匹配，如 1.1 Thermal Shock Test.xlsx / 1.2 ORT Burn-In Test Report.xlsx / 2.1 Conducted EMI Measurement.xlsx）；整表铺淡灰（索引色 22）、表格及周围一格留白（已有彩色底纹不动）；标题下方补 `Create by: 建立人`；合计行改回模板的合计行样式（原来误用了分类行样式）。
- **新增"测试种类"（测试项目表）**：`test_items_catalog` 增加 `Category`；管理→测试项目新增「测试种类」列与「自动归类」按钮，新增/编辑对话框可选可填。归类规则：关键词优先（EMC 关键词含 EMI/EMC/ESD/Surge/Harmonic/Burst/Fast Transient/Conducted 等；可靠性含 Thermal/Burn/Vibration/Drop/Humid/HALT/MTBF/Tumble 等），关键词认不出时取计划索引归并出来的历史分类，仍认不出归「不确定」，由用户手工归类；已归类（含手工）的不被自动覆盖。报告模板工具的 ORT Plan 与 TestStatus 都按测试种类分组（同类测试项排在一起），Conducted EMI Measurement 归 EMC。
- **ORT Plan 配图与公司 logo**：建立计划索引时顺手抽出历史报告 ORT Plan 表里的图片，按图片锚点行归到对应测试项（落盘到数据库目录的 `PlanImages`，记录在 `plan_item_images` 表；同一测试项以最新一份报告为准），生成模板时把配图放回测试项所在行；另从其他三张表里取公司 logo 补到 ORT Plan 标题左边（历史报告本来就有这张图，模板里缺）。
- **报告模板工具**：测试项表格强制按开始日期排序，禁止点表头排序（`CanUserSortColumns=False`，拖动排序后重新排期同样按开始日期归位）。
- **其它**：`ExcelNpoi` 增加 `NormalizeFormulas` / `ApplyFillOverlay` / `AddPictureAnchored` / `PictureDetails`（图片带锚点尺寸），`SetFormula` 改为先清空再写；`OrtPlanParser` 给每行记录行号并提供 `FindSheet`；四个资源文件各新增 5 个本地化键。
- **验证**：用未随程序发布的控制台冒烟程序（`_dbg`，不参与提交）以真实模板 + 3 个测试项 + 两个工令生成报告模板，核对：Waterfall 日期/星期/UUT 三行（E 列起始日期 + 公式顺推 + m/d 格式）、两个工令分两行、测试项合并与格式；TestStatus 分组为 ENVIRONMENT TESTS（1.1 Thermal Shock / 1.2 Burn In）与 EMC（2.1 Conducted EMI）、三条超链接分别指向对应报告、表格外索引色 22 灰底而表格与周围一格白色、`Create by: Dapper`；ORT Plan 分组 + logo + 6 张测试项配图（Thermal 1 / Burn In 1 / EMI 3，竖向排布）。另用真实库副本跑自动归类：53 个测试项目里 Conducted EMI Measurement / ESD / Surge / Harmonic / Burst → EMC，HALT / DMTBF / Tumble / Rel Test → RELIABILITY TEST，ATE / Microsection 等 → 不确定。
- **测试种类纳入管理**：新增字典表 `test_categories`（`Models/AdminCatalogs.cs` 的 `TestCategory`）与管理窗口「测试种类」页签（增/改/删 + 刷新，内置 RELIABILITY TEST / EMC / 不确定 三种并把测试项目里已用到的种类自动补登记）；测试项目编辑对话框的「测试种类」下拉改取该字典；种类改名会同步测试项目上的归类，删除种类时相关测试项目退回「不确定」。
- **Waterfall 表格边框按历史报告重做**：测试项目安排区（B7 到"最后一天列 + 最后一个序列号行"）整体加框——最外一圈**粗黑线**、内部统一**细灰线**（`ExcelNpoi.ApplyBlockBorder` 在保留字体/底纹的前提下叠加边框）；排期用不到的末尾列整列清干净（连模板残留的黑底与边框一起去掉），不再出现右侧黑白参差。
- **TestStatus 背景真正铺成淡灰**：定位到"大部分还是白底"的原因——模板的淡灰背景来自**列默认样式**（`<col min="14" max="16384" style="9"/>`，样式 9 = 索引色 22），NPOI 写回时会丢掉这条"到最后一列"的默认样式，于是表格右侧与下方变白。改为显式铺底：先把画布（表格 + 40 行 × 28 列）整体刷成索引色 22（连模板里原有的白底一起换掉，只保留黑表头/蓝色分类行这类彩色底纹），再把"表格 + 周围一格"刷白。
- **报告模板工具：测试项可选其他模板方案并可直接编辑**：抽样计划/测试条件/通过判定三个文本框改为可编辑（改完立刻写回该测试项，生成报告用的就是这里的内容）；新增「选择其他模板方案」——列出该测试项的共用模板与各机种计划里的同类写法（有效值 = 差异 ?? 共用模板，标题标出差异字段），选中即套用（含试验周期，会重新排期）；另有「恢复共用模板」。新增 Admin_TestCategoryTab / ReportTemplate_PickVariant 等 17 个本地化键。
- **验证（本轮）**：用真实模板 + 真实库副本生成报告模板，核对 Waterfall 边框（B7 左/上 Medium、内部与右侧/底部 Thin、M9 右/下 Medium）、末尾列 N/T 已无边框与黑底、测试计划区右侧不再有黑白参差；TestStatus 第 1~14 行为白底（含黑色表头与蓝色分类行）而第 15 行起整片 C0C0C0 淡灰；测试种类字典首次运行自动写入三种、下拉可取；用造出来的"共用模板 + 两个机种差异"数据验证模板方案列表（共用模板 + TEST-A 与共用模板相同 + TEST-B 差异 TestCondition,Period，有效值正确）
- **修复 Waterfall 表头文字丢失**：清空日程区时误从 C 列开始清（把模板表头块的 `UUT S/N`(C3)、`Config`(C4)、`S/N`(D4) 一起清掉了），且 `RemoveMergesInRows` 把表头块的合并（B3:B5 / C3:D3 / C4:C5 / D4:D5）也一起删了。现在表头三行只清 E 列起的日程区、合并只从第 6 行起清理，表头块与文字原样保留。
- **Waterfall 日期列按内容自适应宽度**：新增 `EnsureItemColumnWidths`——按"每个测试项合并跨度的总宽 ≥ 它名字的宽度（全角按 2 计）"补足列宽，不够时把差额平摊到跨度内的各列，避免名字溢出到相邻单元格造成重叠（历史报告的日期列就是这样按内容拉开的）；`ExcelNpoi` 增加 `ColumnWidthInChars` 取列宽。
- **TestStatus 表格边框与 Waterfall 一致**：表格区（B5 到最后一个测试项行、到 L 列）最外一圈粗黑线、内部细灰线，与 Waterfall 的加框规则相同。
- **修复设置窗口左右错位**：右侧详情面板里「计划索引」原先排在「邮件服务/邮件模板」之前，与左侧目录（计划索引在最后）顺序不一致；把该节移到邮件模板之后，右侧顺序与左侧目录完全对齐（UI → 主题/语言/字体 → 路径各项 → 邮件服务 → 邮件模板 → 计划索引）。
- **主界面新增「用户」菜单**：原来的「登录/注销」入口移进「用户」菜单，并在其中新增「用户中心」；「退出」仍单独留在菜单栏。
- **新增「用户中心」窗口**（`Main/Views/WindowUserCenter`）：管理当前登录用户自己的资料——账号信息（登录名/身份/创建时间，只读）、基本资料（显示名、邮箱，可改；显示名留空则显示登录名）、密码（没设过密码时是「设置密码」，已设过时是「修改密码」，改密码需先验证当前密码；留空表示不改）、本机登录（显示 cookie 到期时间、可一键清除本机保存的登录信息）。注销后会自动关闭该窗口。
- **登录规则**：账号**还没设置密码**时，只给用户名（密码留空）也允许登录，登录成功后弹窗提示「账号还没有设置密码，是否现在设置？」，确认即打开用户中心；账号已设置密码时仍按散列校验（admin 等有密码账号不受影响）。登录窗口密码框下方加了「账号未设置密码时，密码留空即可登录」的提示，密码留空而登录失败时给的是「该账号需要密码」的提示。
- **管理端配合**：新增用户时密码可以留空（＝暂不设密码，该账号凭用户名登录后由本人设置）；「重置密码」留空表示**清除密码**（该账号改为凭用户名登录，不再发"密码已变更"通知），并给出对应的提示文案。`AuthService` 增加 `HasPassword` / `NeedsPasswordSetup` / `PasswordlessLogin` / `VerifyCurrentUserPassword` / `SetCurrentUserPassword` / `SaveCurrentUserProfile`。
- **验证**：用真实库副本跑认证链路——无密码账号 `Login("nopass","")` 成功且 `NeedsPasswordSetup=true`；设置密码后空密码登录失败、新密码登录成功、`NeedsPasswordSetup=false`；资料保存（显示名/邮箱落库）与非邮箱格式拒绝；admin 空密码登录失败、正确密码登录成功且不提示设密码；管理端"新建不设密码账号"可凭用户名登录、短密码被拒、"清除密码"后仍可凭用户名登录。
- **报告模板工具**：两条分隔条原来没设 `ResizeDirection/ResizeBehavior`（且背景透明），实际拖不动；现在设为 `Rows` +（上：`Next`、下：`PreviousAndNext`）并画成可见的细条（鼠标变上下箭头），**测试计划区默认高度 170 → 300**（最小 140）。
- **模板里的 setup_info.json 删除**：从 `Templates\# ORT Test Report (WK#)_#` 里删掉这个旧版遗留设置文件，生成模板时也不再复制（`IsLegacyFile` 过滤，同样会跳过 Excel 临时文件）。
- **计划表新增「更新报告文件夹」**（工具菜单与工具栏各一个入口）：重新扫描报告根目录、刷新 `report_links` 与计划表上的"报告"标记，扫描完成提示匹配到的报告夹数量；另外**报告模板工具窗口关闭后会自动更新一次**，新生成的报告文件夹立刻能在计划表里看到（以前只在启动且无缓存时扫一次，必须重启程序）。
- **ORT Plan 版式**（图一）：
  - 测试项配图移到**表格右侧的照片列（H 列）**并在行内竖排（每个测试项最多 2 张、最大 200×110px），不再压住 TEST CONDITOIN / PASS CRITERION 的文字；按"图片总高 vs 文字估算高度"设置行高，相邻项目也不会互相遮挡。
  - **序号（NO.）与测试名称（TEST ITEMS）居中**；分类行的序号与分类名同样居中。
  - 大标题「Ongoing Reliability Test Plan」：**加粗、24 号、颜色 RGB(0,0,204)**；表头第 4 行**黑底白字、加粗、居中**；第 5 行行高 **5px**；**测试种类行底纹 RGB(197,217,241) 并加粗**。
  - **TEST CONDITOIN / PASS CRITERION 里每行冒号（: 或 ：）前的词组加粗**（富文本分段，`ExcelNpoi.SetRichText`）。
- **其它**：`ExcelNpoi.CellStyleSpec` 增加 `FontRgb`（自定义字体颜色，之前只支持字号/加粗/填充）；4 个资源文件各新增 5 个本地化键。
- **验证**：生成报告模板后核对——输出文件夹里**没有** setup_info.json（模板副本里仍有该文件，说明是生成时跳过的）；ORT Plan 标题 24pt 加粗 #0000CC、表头黑底白字居中、第 5 行 75twips（≈5px）、分类行底纹 C5D9F1 加粗居中、B/C 列居中、E/F 富文本确实按行把冒号前词组写成 `<b/>` 段（如"**· UUT State**: Non-Operational"）、配图锚点在 H 列且行高被撑到能放下图片。
- **修复「建立报告模板」窗口打不开**（报 `XamlParseException`，内层 `Next 不是 GridResizeBehavior 的有效值`）：上一轮给分隔条设的 `ResizeBehavior="Next"` 不是合法枚举值（`GridResizeBehavior` 只有 BasedOnAlignment / CurrentAndNext / PreviousAndCurrent / PreviousAndNext），窗口在解析 XAML 时就直接抛异常。现在删掉上方那条多余的分隔条（它夹在工具条与测试项目表格之间，拖不动任何一列），只留测试计划区那条 `PreviousAndNext` 的分隔条——拖它即可调整"测试项目表格 ↔ 测试计划区"的高度。
- **导入计划表不再把 Excel 公式写进客户栏**（图二里客户名称显示 `IF(ISBLANK(E117),"",)` 这类公式）：
  - 原因：Schedule 表的"客戶別"是 `IF(ISBLANK(E117),"",VLOOKUP(MID(E117,8,3),'Cust. Code'!…))`，VLOOKUP 取不到值时缓存结果是**错误值**，而 `ExcelNpoi.CellText` 对错误值会落到 `DataFormatter`，NPOI 在这个分支返回的是**公式原文**。
  - `ExcelNpoi.CellText` 改为严格按缓存结果取值：`CellType.Error` 返回 Excel 错误文本（`#N/A` / `#REF!` …），公式没有缓存结果时返回 `#N/A`，取值抛异常时也绝不返回公式原文。
  - `PlanExcelService` 增加 `IsFormulaText`（`=` 开头或含 IF/ISBLANK/IFERROR/VLOOKUP/… 函数调用的文本）作为兜底，导入时命中就按 `#N/A` 落库；另增加 `FixFormulaTextValues`，把历史导入已经写进库的公式脏数据（plans 各文本列 + customers 字典，`#N/A` 客户只保留一条）统一改写成 `#N/A`——导入计划表之后、管理窗口「从计划数据同步」时都会自动跑一遍。
- **ORT Plan 配图放回"原来那一格"**（按"图片放到原来单元格的内容下方，单元格设高一点，图片宽度略小于单元格做到铺满"调整）：
  - 建立索引时记下图片在历史报告里的锚点列（`plan_item_images` 新增 `AnchorColumn` / `AnchorColumn2`），生成时把配图放回那一格（取不到锚点时用 E 列 TEST CONDITOIN），位置在该格**文字的下方**，不再压住 TEST CONDITOIN / PASS CRITERION 的文字。
  - 图片按"单元格宽度 − 两侧各 6px"等比缩放并横向居中（铺满整格，跨列锚点按列宽定位到正确的列），单张高度上限 320px；行高取"文字 + 各张图片 + 间距"，并把 Excel 行高上限（409.5pt）内的空间按张数均分。
  - 表格右侧不再需要照片列，H 列宽度改回历史报告的 2.125。
- **验证**：生成的 ORT Plan 里配图锚点都在 E 列（历史里跨列的那张落在 E:F，终点偏移 264px），纵向起点在文字高度之下（第 7 行从 58px 处开始、第 8 行 E 列从 40px 处开始、第二张接在第一张下方），宽度 = 单元格宽 − 12px（E 列 213px、E:F 段 483px），行高 303 / 315 / 97.5pt 且都写在 409.5pt 以内；用真实计划表文件 `_Y2026 ORT Test Schedule.xlsx` 跑导入：286 条计划里公式脏数据 0 条、28 条客户为 `#N/A`，故意插入的公式脏数据（plan + customer）也被清理成 `#N/A`；错误值公式单元格（r117/r120）的 `CellText` 现在返回 `#N/A` 而不是公式原文。报告模板工具窗口改为在探针里实例化校验：XAML 解析通过（探针环境缺少 DI 容器，异常停在 `GetRequiredService` 而不是 `XamlParseException`）。
- **「数据库路径」改为「数据文件夹」**（原设置只挪了数据库文件，附件 OleFiles / 配图 PlanImages 仍在程序目录，远程库实际是"半迁移"状态）：现在**所有业务数据都放在同一个数据文件夹里**——`ort_plans.db`（含界面/邮件/各业务路径等设置）、`OleFiles`（SN 附件）、`PlanImages`（计划配图）；该文件夹可以是本地目录，也可以是 `\\BNT56\共享\目录` 这类远程文件夹，多台电脑填同一个文件夹即共用同一套数据（本机设置仍各存各的）。`DatabaseService` 统一以数据文件夹推导三条路径，路径规范化/可写性检测见新增的 `Utils/FolderUtil`。
- **网络共享下的 SQLite 日志模式**：SQLite 的 WAL 依赖共享内存映射，在 SMB 共享上不可用（原来无条件 `PRAGMA journal_mode=WAL`，远程库会有风险）。现在按文件夹类型自动选：本地目录用 WAL；网络路径（UNC 或映射的网络驱动器）改用 `journal_mode=TRUNCATE` + `synchronous=FULL`，并统一设置 `busy_timeout=30000`，多台电脑同时写先排队重试而不是直接报 database is locked。
- **数据文件夹不可用时可读地失败**：设置里改完会先创建目录并试写一个临时文件验证（不通过就提示原因且不保存）；启动时数据文件夹不可访问（网络不通/无权限）会弹窗说明并给出"改程序目录下 Data\local_settings.json 的 DataFolder"的修复办法，而不是带着半个库继续跑。
- **更换数据文件夹后可一键搬迁 + 一键重启**：在设置里改完数据文件夹并保存后，若新文件夹里还没有数据库，会问"是否把当前数据（数据库/附件/配图）复制过去"（复制前先 `wal_checkpoint(TRUNCATE)`，目标已存在的文件不覆盖，避免踩掉共享文件夹里别人正在用的数据；本机设置文件不复制）；随后提示"需要重启才能生效，是否立即重启"，选"是"即启动新实例并退出当前实例（`App.RestartApplication`），选"否"则明确提示尚未生效。
- **本机设置合并成一个文件**：原来 `local_settings.json`（数据路径）、`auth_cookie.json`（本机登录）、`plans_layout.json`（计划表列布局）各存一个文件，现在统一为 `Data\local_settings.json` 一个文件（`Models/LocalSettings` + `Services/LocalSettingsStore`）：`DataFolder` / `AteDataPath` / `EmiDataPath` / 本机登录（用户名 + DPAPI 加密密码 + 到期时间）/ 计划表列布局。启动时读取，**缺失项一律用默认值**；旧格式文件（含更早的 `db_path.json`）会自动迁移并删除旧文件。
- **验证**：用真实的本机设置文件跑迁移——旧 `{"DatabasePath": "...\bin\Debug\Data"}` 被读成 `DataFolder` 并重写成新格式，`auth_cookie.json`（用户名 123 + DPAPI 密码 + 到期时间）与 `plans_layout.json`（两个表格各 16/13 列的布局）都并进同一个文件，两个旧文件删除后只剩 `local_settings.json`；`ResolveDataFolder` 对相对路径按程序目录解析；`\\BNT56\ORT\Data`、`\\192.0.2.1\share\x` 识别为网络路径而 `C:\Temp\ort` 不是；数据文件夹指向 `Z:\nope\x` 时 `DatabaseService` 抛 `DataFolderUnavailableException` 并给出可操作的提示；迁移复制用真实 `bin\Debug\Data` 做源：复制 47 个文件（数据库 1 + OleFiles 31 + PlanImages 15），本机设置三个文件按设计不复制；设置窗口与计划窗口的 XAML 在探针里实例化校验通过。
- **修复：改了「数据文件夹」保存后，重新打开设置显示的还是旧路径**：判断条件写错了——当时用的是"输入框的值 ≠ 打开窗口时的值"**并且**"已保存值 ≠ 本次运行正在使用的值"，而在没有重启的情况下后者恒为假，于是整段保存逻辑被跳过：改了等于没改，连重启提示都不会出现。现在改为直接比"输入框里的路径 **vs** 磁盘上已保存的路径"：不同就保存并提示重启（可选把现有数据复制过去）；相同但"已保存值 ≠ 本次运行使用的值"（上次改过还没重启）时，再提示一次重启、不再问是否复制数据。另外非管理员的数据文件夹输入框与浏览按钮直接禁用（原来可编辑但存不进去，容易误解）。
- **验证（本轮）**：用探针驱动设置窗口本身（真实 DI 容器 + 真实库副本 + admin 登录，走窗口的 `TrySaveDataFolderFromUi` 以免被模态框挡住）——打开#1 显示旧路径 `…\Data` → 改成 `…\_newdata` 保存（返回"需要重启提示=True、问是否复制=True"，磁盘上已变成 `_newdata`）→ 打开#2 显示 `_newdata`（**修复生效**）→ 不改动再保存不会重复问"是否复制数据"（问是否复制=False）→ 改回原值并落盘正常。
- **修复：数据文件夹填网络共享（UNC）时登录不了**（日志 `AuthService | 登录失败 unable to open database file`）：根因是 **SQLite 的 Windows 层打不开 UNC 路径的库文件**——实测（SQLite 3.46.1）无论库是否 WAL 模式、是否加 `\\?\UNC\` 前缀、是否指定 `win32-longpath` VFS、是否 `ToFullPath=false`，只要路径是 `\\服务器\共享\...` 一律 `unable to open database file`；而**映射成网络驱动器盘符后完全正常**（含建库/写读）。于是：
  - 新增 `Utils/NetworkDriveMapper`：数据文件夹填 UNC 路径时，启动时自动把共享映射成一个空闲盘符（已映射的共享直接复用，`WNetGetConnection` 查已映射、`WNetAddConnection2` 新建，只在本次登录会话内有效），之后数据库/附件/配图全部走盘符路径；映射失败（含"已用其他账号连接过该服务器"）会明确报错并给出手工命令 `net use Z: \\服务器\共享 /persistent:yes`，不会让程序带着打不开的库继续跑。`DatabaseService` 增加 `ConfiguredDataFolder`（设置里填的值）与 `DataDir`（实际使用的盘符路径）区分，设置窗口的"是否改动/是否提示重启"比较改用前者。
  - 新增 `Utils/SqliteFileUtil`：读库文件头判断是否 WAL 模式，并能把 WAL 库转成回滚日志模式（先复制到本地临时目录 `PRAGMA journal_mode=DELETE` 把 WAL 内容并回主库，再写回原路径——因为 WAL 库在网络路径上连锁定/打开都做不到）。网络共享上的库启动时若发现是 WAL 会自动转换（老数据不丢）；迁移数据到网络文件夹时也会生成回滚模式的副本（`DataFolderMigrator` 不再直接拷 `-wal`/`-shm`）。
  - 启动时先验证数据库真的打得开（`VerifyOpenable`），打不开就弹窗说明路径、是否网络共享、是否 WAL、SQLite 原始错误与常见原因；启动日志追加当前日志模式（`delete`/`truncate`/`wal`）便于远程诊断。
- **验证（UNC/网络共享专项）**：用本机管理共享 `\\localhost\C$` 造出真实 UNC 场景——① 把本地 WAL 模式的库拷到共享后直接打开，复现出与用户完全一致的 `unable to open database file`；② 走真实 `DatabaseService`（数据文件夹填该 UNC 路径）：自动映射为 `Z:`、`IsNetworkFolder=True`、日志模式 `truncate`、旧数据（`marker` 表里的中文行）仍在、可正常建表写读；③ 数据从本地迁移到另一个网络文件夹时生成的是回滚模式库（`IsWal=False`）；④ `SplitUnc` 对 `\\BNT56\ort\一键报告\data` 正确切出共享根 `\\BNT56\ort` 与相对目录；⑤ `WNetAddConnection2` 用 `[MarshalAs(LPWStr)]` 的 struct 才封送正确（用 class 会返回 1200 指定的设备名无效，已修正）。

## 2026-09-21

- **修复：登录后主界面不出现（程序直接退出）**（用户反馈"在登陆后没有打开主界面"，日志里紧跟着就是「程序退出」）：
  - 根因：登录前置改造后 `App.xaml` 去掉了 `StartupUri`，启动阶段**唯一的窗口是登录窗口**；而 `Application.ShutdownMode` 仍是 WPF 默认的 `OnLastWindowClose`，于是登录窗口一关闭（登录成功 / 游客进入 / 直接点关闭）就被判定为"最后一个窗口关闭"→ 整个程序开始退出，`App.OnStartup` 里随后创建的 `MainWindow` 即使 `Show()` 了也立刻被关掉，用户看到的就是"登录完就没了"。
  - 修复：`App.OnStartup` 起始处把 `ShutdownMode` 设为 `OnExplicitShutdown`，只在三处主动退出——登录窗口选「退出」、数据文件夹不可用、主窗口关闭（`MainWindow.OnClosed` 里本来就有 `Application.Current.Shutdown()`）；同时在创建主窗口后显式登记 `Application.MainWindow = mainWindow`（任务栏、Toast 定位与 Owner 归属都依赖它，登录窗口只是启动阶段的临时窗口）。
  - 验证：新增"启动流程"冒烟程序（`bin\Debug\_dump\appharness`，不参与提交）真正调用 `App.Run()` 并在登录窗口里自动走登录/游客，然后检查主界面是否打开。**修复前**（HEAD 版本重编）：`ShutdownMode=OnLastWindowClose`、`APPLICATION-EXIT 主界面是否已出现过=False`、`RESULT 主界面已打开=False`（登录窗口一关程序即退出，与用户现象一致）；**修复后**：第 3 步「主界面已打开 Title=ORT实验室管理系统 可见=True」、2 秒后「主界面仍在=True」。另直接启动真实 `ORT一键报告.exe` 8 秒仍存活（停在登录前置的窗口上）。
- **领用和计划：搜索栏右侧改为状况统计**：去掉原来的搜索提示文字，改为「进行中 / 预排测试 / 已结案」三个彩色徽标（与状况列同色系），数字按**当前搜索与列筛选后可见的记录**实时统计；`PlansViewModel` 新增 `OngoingCount / PendingCount / ClosedCount` 与 `UpdateStatusCounts()`，在搜索防抖、列筛选、加载/扫描刷新、单元格修改（含状况下拉）五处刷新。新增 4 个本地化键（三个标签 + tooltip）。
- **计划表状况列按值改变底色**：`Plan.StatusKind` 由新增的 `Utils/PlanStatusKind.Of` 现算（Close/Closed/结案 都算已结案，Ongoing/Pending 不区分大小写，认不出的写法返回空＝不上色）；窗口新增 `StatusCell` 样式（`BasedOn` 现有 `FullCell`）用 DataTrigger 上色——**默认（Fluent）进行中黄 `#FFE082`、预排测试红 `#EF9A9A`、已结案绿 `#A5D6A7`**，文字色分别 `#8A6D00 / #B71C1C / #1B5E20`；Material 用同一套，DarkLab 用暗底亮字（`#3A331A/#FFD54F`、`#3A1F1F/#EF9A9A`、`#17301B/#A5D6A7`），`CustomThemeTemplate.xaml` 同步补上这 6 个画刷键（未加入 `RequiredKeys`，旧自定义主题不会因此校验失败，只是不上色）。选中触发器放在最后声明，选中的单元格仍显示选中色。
- **用户的几处界面调整（一并提交）**：主窗口高度 400→500、菜单精简为「查看日志 / 设置 / 退出」；领用和计划的「回线」菜单移入工具菜单；登录窗口高度 300→320；报告表头列宽 125→130；报告窗口的「查看日志」提到一级菜单、一行高度 35→auto。
- **验证**：`bin\Debug\_dump\planharness`（不参与提交）用真实库副本（289 条计划：63 Ongoing / 5 Pending / 221 Close，另把 5 条改成 Pending 以覆盖三种配色）开真实窗口核对——① 计数与库内聚合完全一致；② 搜索 "WAQ" 后计数合计 = 表格可见行数（9），说明统计跟随筛选；③ 三种主题下三种状况的底色/文字色都等于主题画刷、认不出的值不上色、选中时选中色优先（`#E0E0E0` / `#1E3A5A` / `#B2DFDB`）；④ 徽标文案 zh-CN「进行中 63 / 预排测试 5 / 已结案 221」、切到 English 后「Ongoing 63 / Pending 5 / Close 221」。
