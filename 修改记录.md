# ORT一键报告修改记录

> 精简版：按日期保留变更要点，原详细文件清单、现象/根因与逐项验证记录已压缩。完整历史可查看 Git 中的旧版本。

## 2026-08-06

- **修复**：读取报告时模板路径为空不再抛异常，改为跳过表头读取并记录 Warn；修复日志窗口打开时空引用、`LevelCellStyle` TargetType 不匹配导致的崩溃。
- **新增**：日志查看窗口，支持按日期/级别/关键字筛选、排序、自动刷新、异常堆栈并入上一条；主窗口与报告窗口菜单增加入口。
- **重构**：清理空实现；拆分 `Utils/Report.cs`；消除 `Docx2Pdf` 重复代码；完善依赖注入容器；解耦 View-ViewModel 静态依赖；把视图业务逻辑迁移到 ViewModel。

## 2026-08-18

- **新增计划管理模块（Plans）**：SQLite 全局数据库、plans 表、计划/领用表导入导出、OLE 附件提取、权限预留、管理界面与 DI 注册。
- **修复**：导入时 EPPlus OLE 提取抛空引用导致调试器中断，改为优先 zip 直读 embeddings，失败再回退 EPPlus。
- **合并/筛选优化**：改为「计划表基底 + 领用表辅助」单行合并模型；支持数据清空、多维动态筛选、搜索防抖、列拖动与布局持久化、SN 文件点击打开。
- **关联修正**：工令/日期+机种关联规则调整，计划表与领退记录合并率大幅提升。
- **权限与审核**：新增用户/角色/登录/注销、审核工作流、管理模块（人员/客户/测试项目）；回线转移单入口迁移；一键报告按 RT 工号/机种匹配计划数据。
- 所有新窗口与服务完成 DI 注册，MSBuild 编译通过。

## 2026-08-19

- **Excel 风格编辑**：测试项目以计划表为准；新增行/单元格编辑先暂存，手动提交统一写库并记录变更日志；右键菜单、编辑校验、字典约束、机种/测试项目联动。
- **修复**：单元格编辑提交时 `InvalidOperationException`（EditItem 事务中 Refresh），实体改为带属性通知的 `ObservableObject`，移除编辑路径中的 Refresh。
- **界面/数据改进**：日期列改 `DateTime?`、单元格垂直居中、列可隐藏/布局持久化、移除界面冗余列；修复主窗口无法置前；客户-产品别整合后按需求撤销并恢复分表。
- **修复**：PowerShell 批量改文件导致编码损坏，使用 `git restore` 恢复后重新应用修改。
- **分表重构**：计划表与领退表拆分为独立实体/表/双 Tab 界面，分别导入导出，新增自动编号、领退/计划新增编辑表单，提审流程支持领退表。
- **浏览编辑分离**：日期与下拉列浏览/编辑分离；表格内测试项目联动；右键单元格直接编辑；清空数据入口迁移到管理模块。
- 日期解析支持「2026/8/18」「8月7日」等格式。

## 2026-08-20

- **修复**：工程文件重复引用；编辑对话框直接写库改为走暂存/提审；机种改名自动带出产品别/客户别；自动补全字段允许手改。
- **代码映射**：新增 `code_mappings` 表，支持同一产品/客户名称对应多个代码；客户别/产品别按机种名代码规则查询；EPPlus 许可统一到程序入口。
- **界面调整**：管理窗口改名与列顺序优化；表格单元格样式/居中/右键菜单完善。

## 2026-08-26

- **修复**：选中单元格文字变白；领退表单 SN 栏支持回车换行与高度自适应；编辑领退记录时自动带入对应计划记录。
- **新增设置页面**：VSCode 风格左树+右详情；JSON 保存 UI 字体/字号与常用目录，支持实时应用；主窗口新增设置入口。

## 2026-08-27

- **设置入库**：设置改为保存到数据库 `app_settings`；数据库路径作为独立本地配置；ATE/EMI 路径也改为程序目录本地文件保存。
- **设置交互**：设置窗口改为保存/取消/应用按钮；数据库路径仅管理员可修改。
- **报告对应**：新增报告夹扫描 `report_links`；计划表右键可打开报告文件夹/概览；计划表按是否有报告区分颜色、行列选中高亮。
- **体验/提速**：引入 Ookii.Dialogs.Wpf 原生文件夹选择；报告扫描异步化并持久化到数据库，大幅提升领退和计划窗口打开速度。

## 2026-08-28

- **窗口行为**：主窗口退出时关闭全部子窗口；领退和计划窗口单实例（已开则聚焦）；注销时自动关闭无权限子窗口。
- **登录体验**：本地 Cookie 保留上次登录，一周内免密续登；修复继续登录按钮溢出。
- **一键报告**：计划表右键打开一键报告并携带计划/领退预填数据；提取 `ReportInputModel` 模型与 `ReportGenerationService` 生成入口；报告 Tab 改为按需懒加载并持久化勾选状态；清理无效菜单。
- **扫描持久化**：报告夹扫描结果落库，打开时不再遍历文件系统，刷新时才重新扫描。

## 2026-09-03

- **多语言本地化**：引入 WPFLocalizeExtension，建立简体中文/繁体中文/English 三语资源；完成全部 XAML、MessageBox 与 code-behind 字符串本地化。
- **语言服务**：新增 `LanguageService`，支持设置中切换语言并持久化。
- **修复**：内置 Provider 对中文程序集名解析失败导致界面显示 `Key:xxx`，新增自定义 `OrtLocalizationProvider` 绕过。
- **UI 主题**：新增 Fluent/Material/DarkLab 三套主题，设置中可运行时切换并持久化。

## 2026-09-04

- **深色主题**：通过 DWM 沉浸式深色模式统一系统标题栏/窗口边框；主题化滚动条与 ToolBar，修复 ToolBar 按钮垂直堆叠。
- **自定义主题**：支持运行时导入用户自定义主题（按语义键校验、自动判定深色标题栏），无需重新编译。
- **可读性修复**：当前行/选中行底色改用语义键 `DynamicResource`，深色主题下不再出现白底浅字。
- **交互新增**：Toast 通知窗口与 `ToastService`；下拉单元格改为浏览文本、双击才出现选择框；报告路径为空时以 Toast 提醒；设置中可配置通知位置。