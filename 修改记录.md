# ORT一键报告修改记录

## 2026-08-06

### 修复

#### 1. 读取报告报错 System.ArgumentException: 路径的形式不合法
- **现象**：当报告同级目录下的 Report 文件夹中不存在对应报告模板时，`GetTemplatePath` 返回空字符串，传入 `new FileInfo("")` 抛出 `ArgumentException`，导致一键报告流程终止。
- **修改文件**：`Reports/Views/UserControlBaseReportPage.xaml.cs`
- **修改内容**：`ReadReportHeader()` 中先校验模板路径是否为空及文件是否存在；不存在时记录 Warn 日志并跳过该报告表头读取，不再抛出异常，流程继续执行。

#### 3. 日志窗口打开时崩溃 System.NullReferenceException (Filter_Changed)
- **现象**：打开日志查看窗口时，`WindowLog.Filter_Changed` 抛出空引用异常。
- **原因**：`InitializeComponent()` 解析 XAML 时，级别下拉框的 `IsSelected="True"` 会提前触发 `SelectionChanged → Filter_Changed`，此时 `CollectionViewSource.Source` 尚未赋值，`View` 为 null。
- **修改文件**：`Main/Views/WindowLog.xaml.cs`
- **修改内容**：`Filter_Changed` 开头增加 `_viewSource?.View == null` 判空，初始化阶段直接返回。

#### 4. 日志窗口显示数据时崩溃 System.InvalidOperationException（CellStyle类型不匹配）
- **现象**：日志列表渲染时抛出“"TextBlock"TargetType 与元素"DataGridCell"的类型不匹配”。
- **原因**：`DataGridTextColumn.CellStyle` 要求样式的 TargetType 为 `DataGridCell`，之前误写为 `TextBlock`。
- **修改文件**：`Main/Views/WindowLog.xaml`
- **修改内容**：将 `LevelCellStyle` 的 TargetType 改为 `DataGridCell`，级别颜色/加粗通过单元格继承属性生效。

### 新增

#### 2. 日志查看窗口
- **新增文件**：
  - `Main/Views/WindowLog.xaml`
  - `Main/Views/WindowLog.xaml.cs`
- **功能**：
  - 按日期选择 `logs/` 目录下的日志文件（默认选中最新）
  - 按日志级别筛选（全部 / DEBUG / INFO / WARN / ERROR / FATAL），不同级别颜色区分
  - 关键字搜索（匹配内容、来源、级别）
  - 点击列头排序（时间 / 级别 / 来源 / 内容，默认按时间降序）
  - 每 2 秒自动刷新（仅在日志文件大小变化时重新加载，可关闭）
  - 异常堆栈等续行自动并入上一条日志
- **入口**：主窗口与报告窗口菜单栏新增「视图 → 查看日志」
- **修改文件**：
  - `MainWindow.xaml` / `MainWindow.xaml.cs`
  - `Reports/Views/WindowMainReport.xaml` / `WindowMainReport.xaml.cs`
  - `ORT一键报告.csproj`（注册新窗口文件）

### 构建验证
- MSBuild Debug 编译通过，无新增错误或警告。

### 重构（代码结构优化）

#### 1. 清理空实现代码
- **删除**：`MainWindow.xaml.cs` 中空的 `Button_Plans_Click` 方法及其 XAML Click 绑定
- **修改文件**：`MainWindow.xaml.cs`、`MainWindow.xaml`

#### 2. 拆分 Utils/Report.cs 大文件
- **新增文件**：`Utils/SmartTableExtractor.cs`（从 Report.cs 提取 `SmartTableExtractor` 类）
- **修改文件**：`Utils/Report.cs`（移除 `SmartTableExtractor` 类及未使用的导入）
- **注册文件**：`ORT一键报告.csproj`

#### 3. 消除 Docx2Pdf 重复代码
- **提取**：`ConvertSingleFile` 私有方法封装 Word→PDF 转换的 COM 操作逻辑
- **修改文件**：`Utils/Docx2Pdf.cs`（两个重载均调用公共方法，消除重复）

#### 4. 完善依赖注入容器
- **新增文件**：`Services/ReportService.cs`（封装 `WindowMainReport` 静态属性）
- **修改**：`App.xaml.cs` 注册所有 ViewModel 和 Service 到 DI 容器
- **修改**：各窗口通过 DI 容器解析 ViewModel，而非手动 `new`
- **修改文件**：`App.xaml.cs`、`MainWindow.xaml.cs`、`Reports/Views/WindowMainReport.xaml.cs`、`Reports/Views/UserControlBaseReportPage.xaml.cs`、`Reports/Views/UserControlEMIReportPage.xaml.cs`、`ORT一键报告.csproj`

#### 5. 解耦 View-ViewModel 静态依赖
- **新建**：`Services/ReportService.cs` 类，封装原 `WindowMainReport` 的静态属性（`RootPath`/`TemplateDir`/`TempPath`/`UUTInfos`）
- **修改**：`EMIReportViewModel`、`BaseReportPageViewModel`、`EMISetupViewModel`、`MainReportViewModel` 构造函数注入 `ReportService`，替换 `WindowMainReport.XXX` 静态引用
- **修改**：`Utils/Report.cs` 中 `ExcelAddPicture` 方法增加 `tempPath` 参数
- **修改文件**：`Services/ReportService.cs`、`Reports/Views/WindowMainReport.xaml.cs`、`Reports/ViewModels/EMIReportViewModel.cs`、`Reports/ViewModels/BaseReportPageViewModel.cs`、`Reports/ViewModels/EMISetupViewModel.cs`、`Reports/ViewModels/MainReportViewModel.cs`、`Utils/Report.cs`

#### 6. View 业务逻辑迁移到 ViewModel
- **迁移**：将 `WindowMainReport.xaml.cs` 中的 `ReadInfoFromOverview` 方法及其嵌套函数（`ReadInfosFromReport`、`FindTestItems`、`FindSNs`）迁移到 `MainReportViewModel`
- **修改**：`WindowMainReport.xaml.cs` 中的 `DoReport_Click` 事件改为调用 `MainVM.ReadInfoFromOverview(ReportName)`
- **修改文件**：`Reports/ViewModels/MainReportViewModel.cs`、`Reports/Views/WindowMainReport.xaml.cs`

### 构建验证
- MSBuild Debug 编译通过，0 错误，7 警告（均为绑定重定向及未使用字段的已有警告）

## 2026-08-18

### 新增：计划管理模块（Plans）

#### 1. 全局 SQLite 数据库
- **新增文件**：`Services/DatabaseService.cs`
- **说明**：采用 FreeSql + SQLite（项目已引用 `FreeSql.Provider.Sqlite`），数据库文件保存在程序根目录 `Data\ort_plans.db`，附件保存在 `Data\OleFiles`；启用 WAL 模式，连接池上限 10（满足最大并发 10 人次）；`UseAutoSyncStructure` 首次运行自动建表。

#### 2. plans 表结构
- **新增文件**：`Models/Plan.cs`
- **说明**：一张宽表整合两表数据，`SourceType` 区分来源（领用表/计划表）；自增 `Id` 主键，三个业务唯一索引：`领料单据号`、`回线RT工令`、`工作編號`（空值存 NULL，SQLite 唯一索引允许多个 NULL）；含审计字段 `CreatedBy/CreatedAt/UpdatedBy/UpdatedAt` 预留用户管理对接。

#### 3. 导入/导出/OLE 提取服务
- **新增文件**：`Services/PlanExcelService.cs`
- **导入**：上传原样领用表（成品領用記錄）/计划表（ORT Test Schedule），动态定位表头行并按列读取；重复项按唯一键匹配合并（领用表先按领料单据号、再按回线RT工令匹配，命中则更新）。
- **OLE 提取**：领用表 S/N 列的嵌入对象保存到 `Data\OleFiles`，命名 `{简短日期}_{领用单据号}_{机种名称}`（简短日期=月日4位，如 0109）；优先 EPPlus API，对 ProgId="工作表" 等 EPPlus 提取失败的对象回退为直接解析 xlsx 包内 embeddings 数据；扩展名按文件头判断（PK→.xlsx，D0CF→.xls，%PDF→.pdf）。
- **导出**：从数据库重新导出为领用表（退管理表格式，SN 文件以 OLE 对象嵌回 S/N 列）和计划表（Schedule 格式）。
- **许可**：静态构造函数中设置 EPPlus 非商业许可，避免非 GUI 入口未设置许可报错。

#### 4. 权限预留
- **新增文件**：`Services/PermissionService.cs`（`IPermissionService` 接口 + 默认全放行实现）
- **说明**：导入/导出/新增/删除均经过 `Can("plan.xxx")` 检查，下次任务接入用户管理时替换 DI 实现即可；审计字段自动记录当前用户名。

#### 5. 计划管理界面
- **新增文件**：
  - `Plans/ViewModels/PlansViewModel.cs`（列表、来源筛选、关键字搜索、各操作命令）
  - `Plans/Views/WindowPlans.xaml(.cs)`（工具栏 + 筛选栏 + DataGrid + 状态栏）
  - `Plans/Views/WindowPlanEdit.xaml(.cs)`（新增对话框：来源切换字段组；序列号栏支持自定义输入/上传文件二选一；唯一键重复拦截）
- **删除**：删除前弹出确认框（显示单据号/工作編號与机种）。
- **入口**：`MainWindow` 的“计划管理”按钮恢复 Click 绑定，打开计划管理窗口。

#### 6. DI 与工程注册
- **修改**：`App.xaml.cs` 注册 `DatabaseService`（单例）、`IPermissionService`、`PlanExcelService`、`PlansViewModel`；退出时 Dispose 容器释放数据库连接；清理重复 using。
- **修改文件**：`App.xaml.cs`、`MainWindow.xaml(.cs)`、`ORT一键报告.csproj`（注册 7 个新文件）

### 修复

#### 7. 导入时调试器中断于 EPPlus 的 NullReferenceException（GetEmbeddedObjectBytes）
- **现象**：导入领用表时 Visual Studio 在 `ExcelOleObject.GetEmbeddedObjectBytes()` 抛出的一级异常处中断（异常实际已被捕获并回退处理，但抛异常本身会触发调试器中断）。
- **原因**：EPPlus 对 ProgId="工作表" 等对象调用 `GetEmbeddedObjectBytes()` 必抛空引用；原逻辑先调 EPPlus API 再回退 zip 直读，异常噪声无法避免。
- **修改文件**：`Services/PlanExcelService.cs`
- **修改内容**：`SaveOleObject` 提取顺序调整为优先 zip 直读 embeddings（稳定无异常），仅当 zip 直读失败时才回退 EPPlus API（仍包在 try/catch 中）。

### 验证
- 回归测试：领用表导入 232 条（196 新增 + 36 按回线RT工令合并更新），31 个 OLE 对象全部经 zip 直读提取成功，无任何异常日志。
- 用真实文件端到端验证：领用表导入 232 条、计划表导入 586 条；二次导入全部命中更新（0 新增）；31 个 OLE 对象全部提取成功且命名符合 `{月日}_{单据号}_{机种}.xlsx`；导出的领用表成功将 SN 文件重新嵌入为 OLE 对象；无唯一约束冲突、无异常。
- MSBuild Debug 编译通过，无新增错误；测试用临时工具与测试数据已清理（表结构保留，数据初始化按要求后续进行）。

### 优化：合并模型与筛选增强

#### 8. 改为"计划表基底 + 领用表辅助"的单行合并模型
- **需求**：两表数据不再拆分为不同行、不按来源区分，尽量补全同一行数据（两表一般工令与机种名称一致）。
- **修改**：
  - `Models/Plan.cs`：移除 `SourceType` 字段；新增 5 个可空日期解析字段（领用/回线/入库/开始/结束），原文日期字段保留以便原样导出。
  - `Services/PlanExcelService.cs`：领用表导入匹配顺序为 领料单据号 → 回线RT工令 → WorkOrder → 机种+测试项目+数量；计划表导入匹配顺序为 工作編號 → 备注中包含的 WorkOrder → 机种+测试项目（仅限无工作編號的行）；备注字段以计划表为优先；日期文本（如"1月9日"，年份从导入文件名推断）解析入日期字段。
  - 导出改为按字段存在性取数：领用表导出有领料单据号的行，计划表导出有工作編號的行。
  - `Plans/Views/WindowPlanEdit.xaml(.cs)`：取消来源切换，统一为"计划信息+领用信息"单行表单，唯一键（工作編號必填/领料单据号/回线RT工令）重复拦截。
  - `PlansViewModel`/`WindowPlans`：移除来源筛选与来源列。

#### 9. 数据持久化与全部清空
- 数据持久化于 SQLite（`Data\ort_plans.db`，随程序目录保留）；工具栏新增"全部清空"按钮，需二次确认后调用 `PlanExcelService.ClearAll()`（数据库文件保留、表结构不变）。

#### 10. 多维筛选
- 筛选栏新增：机种/测试项目/线别/产品别/客户别/负责人/阶段/状况 8 个下拉（选项由当前数据动态去重生成，首项"全部"）+ 日期范围选择器（按领用日期/开始日期筛选）；保留关键字搜索。

#### 11. 列拖动重排与顺序持久化
- DataGrid 开启 `CanUserReorderColumns`，用户可拖动列头调整顺序；窗口关闭时将列顺序保存到 `Data\plans_layout.json`，下次开启自动恢复。

### 验证（合并模型）
- 真实文件回归：领用表 232 行导入后合并为 97 行；计划表导入后 93 行与领用行成功合并到同一行（同时含工作編號与领料单据号），最终 290 行；日期解析正常（领用 97 条/开始 286 条）；`ClearAll` 清空 290 条后为 0。编译通过，临时工具与测试数据已清理。

### 改进：合并关联修正与交互优化

#### 12. 领用表与计划表的工令关联合并修正
- **问题**：两表数据仍未有效合并。计划表的工令实际放在备注列；此前领用表导入的"机种+测试项目+数量"兑底匹配会把不同领用单过度合并，导致工令丢失、计划表无法关联。
- **修改内容**（`Services/PlanExcelService.cs`）：
  - 领用表导入取消"机种+项目+数量"兑底匹配，仅按 领料单据号 → 回线RT工令 → WorkOrder 匹配，保留每行工令；并支持反向关联（先导入计划表时，按备注中包含的工令合并）。
  - 计划表导入从备注列提取工令与领用行关联，改为双向包含匹配（备注中工令可能被截断），且工令匹配不再限制机种。
  - 备注无工令且工作編號非 Q 开头（Q 开头为可靠性试验计划，通常无领用记录）的行，导入后弹窗列出提示（超过 30 条时截断显示）。

#### 13. SN 文件可点击打开
- 列表"SN文件"列改为超链接（DataGridTemplateColumn + Hyperlink），点击调用系统默认程序打开附件；文件不存在时提示。`PlansViewModel` 新增 `OpenSnFileCommand`。

#### 14. 筛选折叠区域
- 筛选栏（8 个下拉 + 日期范围 + 搜索）包入 `Expander`（默认展开，可折叠收起）。

### 验证（工令关联）
- 真实文件回归：领用表 232 行导入为 226 条（仅按唯一键去重）；计划表导入后 **204 条**与领用行合并到同一行（修正前仅 93 条）；29 条备注无工令且非 Q 开头的记录正确进入提示列表（抽查确认这些行备注确实为空）。编译通过，临时工具与测试数据已清理。

### 改进：日期判定合并与筛选交互重构

#### 15. 无工令数据的日期补充判定
- **规则**：无法按工令关联时，若领用日期与开始日期一致且机种相同，则判定为同一记录合并（双向：计划表导入和领用表导入均支持）。
- **修改文件**：`Services/PlanExcelService.cs`（`FindScheduleMatch`/`FindExistingRow` 增加日期+机种判定环节）。

#### 16. S/N 列与 SN 文件列合并
- 合并为单个"S/N"列：无附件时显示 SN 文本，有附件时显示可点击的文件超链接（悬停提示 SN 文本，点击打开文件）。
- **修改文件**：`Plans/Views/WindowPlans.xaml`。

#### 17. 搜索卡顿优化
- 搜索框改为 250ms 防抖（DispatcherTimer）：停止输入后才触发一次全量筛选刷新，避免逐字符刷新 DataGrid 造成卡顿。
- **修改文件**：`Plans/ViewModels/PlansViewModel.cs`。

#### 18. 按需筛选条件 + 自定义 Excel 风格筛选控件
- **新增文件**：`Plans/Views/FilterDropDown.xaml(.cs)`（可复用控件）：下拉按钮 + 弹出面板（搜索框过滤选项、全选复选框、多选列表），按钮显示选择摘要（全部/单值/N项已选）；`SelectedValues` 空集合表示不过滤。
- **筛选交互重构**：不再一次性铺开全部筛选项；通过"选择字段"下拉按需添加条件行（可删除），字段含 机种/测试项目/线别/产品别/客户别/负责人/阶段/状况（值字段，用 FilterDropDown 多选）与 **领用日期/开始日期**（日期字段，起止 DatePicker，不再笼统合并为一个"日期"）。
- **修改文件**：`Plans/ViewModels/PlansViewModel.cs`（`FilterCondition` 类、`ActiveFilters` 集合、`AvailableFields`/`AddFilterField`/`RemoveFilterCommand`）、`Plans/Views/WindowPlans.xaml`、`ORT一键报告.csproj`（注册控件）。

### 验证（日期判定）
- 真实文件回归：领用表导入 226 条；计划表导入后与领用行合并数由 204 提升至 **224 条**（日期判定新增合并 20 条）；未匹配提示由 29 条降至 **9 条**（其余为同工令被先导入计划行占用或确实无领用数据）。编译通过，临时工具与测试数据已清理。

### 新增：权限管理与审核体系、管理/审核模块、重构

#### 19. 权限与用户体系
- **新增数据表**：`users`（登录名/显示名/密码散列 SHA256+盐/启用状态）、`user_roles`（一用户多身份）。
- **新增文件**：`Services/AuthService.cs`（登录/登出/创建用户/重置密码；首次运行自动创建默认管理员 **admin/admin123**），`Services/PermissionService.cs` 重写为角色矩阵。
- **角色定义**：游客（未登录，仅浏览领退和计划）/ 普通用户（编辑需提交审核）/ 技术员（直接编辑 + 一键报告，预留报告提交审核）/ 审核员（编辑 + 审核，预留报告审核）/ 管理员（全部权限）。
- **登录窗口**：`Main/Views/WindowLogin.xaml(.cs)`；主窗口菜单提供登录/注销，入口按钮按权限自动启用/禁用。

#### 20. 审核工作流模块
- **新增数据表**：`review_requests`（类型/操作/目标Id/摘要/PayloadJson/请求人/状态/审核人/意见/时间）。
- **新增文件**：`Services/ReviewService.cs`（提交/列表/通过并应用更改/驳回），`Review/Views/WindowReview.xaml(.cs)`（请求列表展开于主界面，状态筛选，双击打开详情），`Review/Views/WindowReviewDetail.xaml(.cs)`（详情+格式化JSON+通过/驳回，驳回必填意见）。
- **计划编辑权限分流**：普通用户的新增/编辑/删除不直接写库，生成审核请求；审核员通过后自动应用（新增→Insert、编辑→Update、删除→Delete）。`PlansViewModel` 新增 `EditCommand` 与 `NeedsReview` 分流；`WindowPlanEdit` 支持编辑模式与提审模式（唯一键校验排除自身）。

#### 21. 管理模块
- **新增数据表**：`customers`、`test_items_catalog`（数据源均在计划表）。
- **新增文件**：`Services/AdminService.cs`（人员/客户/测试项目 CRUD，客户可从已导入计划数据同步，测试项目可从计划表文件 Test Items 工作表同步），`Admin/Views/WindowAdmin.xaml(.cs)`（三个 Tab），`Admin/Views/WindowAdminInput.xaml(.cs)`（通用输入对话框，支持密码框）。
- **人员管理**：新建用户/重置密码/启用禁用/多角色勾选应用（管理员调动已登录用户身份；调整自身角色时自动注销重登）。

#### 22. 入口与命名重构
- `MainWindow`：四个入口——一键报告（技术员及以上）、**领退和计划**（原"计划管理"改名，游客可浏览）、管理（管理员）、审核（审核员及以上，按钮显示待审核数）；菜单显示登录/注销与当前身份。
- `Utils/RelayCommand.cs`：`CanExecuteChanged` 挂接 `CommandManager.RequerySuggested`，按钮状态随选中项/权限自动刷新；临时目录清理移至 App 退出时执行。

#### 23. 重构：回线转移单迁移与一键报告数据源
- 回线转移单入口从一键报告"工具"菜单移除，迁至领退和计划窗口工具栏。
- 一键报告数据源：选择报告概览后，按文件夹名称解析 **RT工号/回线工令**（`RT\d+`/`RTAH\d+`）与**机种名称**，在领退和计划中匹配记录（优先工号，其次机种），匹配结果用于补充报告表头空缺信息（项目名=机种+测试项目、阶段、测试人=负责人、测试描述）；`ReportService` 新增 `MatchedPlan`，`MainReportViewModel` 注入 `DatabaseService` 实现 `MatchPlanFromFolderName`。

#### 24. 注册与构建
- `App.xaml.cs` 注册 `AuthService/AdminService/ReviewService`；`ORT一键报告.csproj` 注册全部新文件（Models/Review.cs、四个服务、五组窗口）。

### 验证（权限与审核）
- 端到端回归：错误密码拒绝登录；admin 具备 report.use/admin.manage/plan.review；普通用户 plan.edit=True/report.use=False/needsReview=True；游客仅 plan.view；审核流：普通用户提交新增→库中无数据（待审核）→审核员通过后生效（驳回流程正常）；客户/测试项目 CRUD 与重复拦截正常。MSBuild 编译 0 错误；临时工具与测试数据已清理。

## 2026-08-19

### 改进：Excel 风格编辑、暂存提交、校验与字典联动

#### 25. 测试项目以计划表为准
- 领用表导入不再覆盖已有记录的测试项目（`PlanExcelService.ImportRequisition`：`existing?.TestItem ?? testItem`）。
- Id 列隐藏；行号显示在行头（`LoadingRow` 事件，类 Excel）；表格改为可单元格编辑、全网格线、新行淡黄底色。

#### 26. 暂存修改与手动提交（非实时保存）
- 新增 `plan_change_logs` 变更日志表（操作/记录Id/摘要/前后快照JSON/操作人/时间，便于回滚追溯）。
- `PlansViewModel` 新增暂存机制：新增行/删除行/单元格修改均先暂存内存，"提交保存"时统一写库（以快照对比自动检测修改行）并逐条写入变更日志；"丢弃修改"还原；状态栏实时显示未提交修改数。
- 普通用户（需提审）表格只读，仍走对话框提审流程。

#### 27. 右键菜单
- 编辑(对话框) / 标记删除 / 复制行(TSV) / 复制单元格 / 粘贴（从当前单元格起向右向下写入，逐格校验）。

#### 28. 回线转移单移至菜单栏（工具栏）保留，一键报告菜单不再重复。

#### 29. 编辑校验与字典约束
- 新增 `Utils/PlanValidation.cs`：
  - 工作編號：QRT/RT 开头 + 4位年月 + 至少2位从01开始的编号（`^(QRT|RT)\d{4}\d{2,}$`）；
  - 状况：仅 Ongoing/Close/Pending（选择框）；
  - 测试项目/产品别/客户别/阶段：必须存在于字典表，新增种类需到管理模块添加。
- 表格内字典列为下拉选择框，单元格编辑结束时校验格式；`WindowPlanEdit` 对话框同步改为选择框并增加工作編號格式校验。
- 新增字典表：`stages`（阶段名+描述，首次运行初始化 MP/EVT/DVT/PVT/RMA）、`products`（产品别）；管理模块新增"产品别管理"与"阶段管理"Tab。

#### 30. 列联动关系还原（计划表公式）
- 新增 `model_mappings` 机种映射表；导入计划表时收集 机种→产品别/客户别 映射（含 Cust. Code 工作表的 Product Type）。
- 表格/对话框中修改机种名称后自动带出产品别与客户别（仅填充空字段）。

### 重构：导入入口迁移
- 导入领用表/导入计划表按钮从领退和计划工具栏移至管理窗口顶部；导入计划表时一并同步客户/测试项目/产品别/机种映射字典。

### 删减
- 移除所有 ToolTip 中的权限提示文字，仅保留功能提示。

### 验证（Excel 风格与字典）
- 端到端回归：阶段默认初始化 MP/EVT/DVT/PVT/RMA；工作編號/状况校验正反例正确；导入计划表同步字典（客户+20/产品别+24/机种映射+116/测试项目+50）；机种联动映射正确（FSF050-00CG→Sever）；领用表导入后测试项目保留计划表值；变更日志表写入正常；修复表头定位提前返回 bug。MSBuild 编译 0 错误；临时工具与测试数据已清理。

### 修复

#### 31. 单元格编辑提交时崩溃 InvalidOperationException（EditItem 事务中不允许 Refresh）
- **现象**：在表格内编辑机种名称（或切换单元格提交）时抛出“在 AddNew 或 EditItem 事务过程中不允许 Refresh”；延迟 BeginInvoke 方案无效（回调仍在提交事务的消息泵内执行）。
- **根因**：机种联动带出产品别/客户别后依赖 `Items.Refresh()` 强制重绘；而 `Plan` 为无通知的 POCO，属性变化无法自动刷新单元格。
- **最终方案**：
  - `Models/Plan.cs`：实体改为继承 `ObservableObject`，全部属性改用 `SetProperty` 实现属性通知；机种联动赋值后单元格自动刷新，彻底移除编辑路径中的 `Items.Refresh()`（含粘贴后的刷新）。
  - `Plans/Views/WindowPlans.xaml.cs`：联动改为同步调用 `AutoFillByModel`，不再调度刷新。
- **验证**：回归确认 PropertyChanged 正常触发、FreeSql 插入/查询/更新与 JSON 快照对比（暂存机制）均兼容；MSBuild 编译 0 错误；测试数据已清理。

### 改进：日期类型、垂直居中、列隐藏、主窗口置前、客户产品别整合

#### 32. 日期列改为 DateTime 类型
- **修改文件**：`Models/Plan.cs`、`Services/PlanExcelService.cs`、`Plans/ViewModels/PlansViewModel.cs`、`Plans/Views/WindowPlans.xaml`、`Plans/Views/WindowPlans.xaml.cs`、`Plans/Views/WindowPlanEdit.xaml`、`Plans/Views/WindowPlanEdit.xaml.cs`
- **修改内容**：
  - `Plan` 实体的 5 个日期字段（RequisitionDate/ReturnDate/StockInDate/StartDate/EndDate）从 string 改为 DateTime?，移除冗余的 *Value 字段
  - 导入时直接解析为 DateTime?（领用表日期从文件名推断年份）
  - 导出时 DateTime? 直接写入单元格（EPPlus 自动格式化）
  - 筛选逻辑直接使用日期字段
  - 表格日期列改为 DatePicker 模板列
  - 对话框领用日期改为 DatePicker
  - 复制/粘贴支持 DateTime? ↔ string 转换

#### 33. 表格垂直居中
- **修改文件**：`Plans/Views/WindowPlans.xaml`
- **修改内容**：DataGrid.CellStyle 设置 VerticalContentAlignment=Center 和 VerticalAlignment=Center，所有单元格内容垂直居中显示。

#### 34. 列可隐藏
- **修改文件**：`Plans/Views/WindowPlans.xaml`、`Plans/Views/WindowPlans.xaml.cs`
- **修改内容**：右键菜单新增"显示/隐藏列"子菜单，动态生成所有列的勾选框，点击切换列的 Visibility。列顺序和可见性持久化到 `Data\plans_layout.json`。

#### 35. 移除创建人和更新时间列
- **修改文件**：`Plans/Views/WindowPlans.xaml`
- **修改内容**：删除"创建人"和"更新时间"两列（审计字段保留在实体中，仅不在界面显示）。

#### 36. 主窗口置前 BUG 修复
- **现象**：主窗口无法带到最上层，被其他窗口遮挡。
- **根因**：子窗口设置 Owner = this 后，主窗口始终在子窗口下方。
- **修改文件**：所有 .cs 文件（批量移除 `Owner = this`）、`MainWindow.xaml.cs`
- **修改内容**：
  - 移除所有窗口的 `Owner = this` 设置（窗口独立，不强制层级关系）
  - MainWindow.Loaded 事件中添加 `Activate()` 确保启动时置前

#### 37. 客户-产品别整合（Cust. Code 对应关系）
- **需求**：产品别和客户别应在同一表中管理，存在对应关系（来自计划表 Cust. Code 工作表）。
- **修改文件**：`Models/Review.cs`、`Models/AdminCatalogs.cs`、`Services/AdminService.cs`、`Admin/Views/WindowAdmin.xaml`、`Admin/Views/WindowAdmin.xaml.cs`、`Plans/ViewModels/PlansViewModel.cs`、`Plans/Views/WindowPlanEdit.xaml.cs`
- **修改内容**：
  - 移除 `Product` 实体类（products 表）
  - `Customer` 实体新增 `Product` 和 `Code` 字段（整合客户别、产品别、客户代码）
  - `AdminService.GetProducts()` 改为从 Customer 表去重获取 Product 字段
  - `AdminService.SaveCustomer()` 支持保存 Product/Code
  - `AdminService.SyncCatalogsFromScheduleFile()` 解析 Cust. Code 工作表（ENDCUSTOMER→Name, PRODUCTTYPE→Product, Cust.Code→Code）
  - 管理窗口合并"产品别管理"Tab 到"客户管理"Tab（显示客户名称/产品别/客户代码/备注）
  - PlansViewModel/WindowPlanEdit 的产品别下拉从 Customer 表去重获取

### 修复：PowerShell 批量操作导致文件编码损坏

#### 38. 文件编码损坏恢复
- **现象**：使用 PowerShell `Set-Content -Encoding UTF8` 批量移除 `Owner = this` 时，所有 .cs 文件被重新写入导致编码损坏（UTF-8 BOM 重复或其他编码问题），编译报大量"意外的字符"错误。
- **修复**：使用 `git restore` 恢复被损坏的文件（排除本次会话有意修改的文件），然后重新应用本次会话的修改。
- **受影响文件**：Main/Views/WindowLog.xaml.cs、Main/Views/WindowLogin.xaml.cs、Admin/Views/WindowAdminInput.xaml.cs、Review/Views/WindowReview.xaml.cs、Review/Views/WindowReviewDetail.xaml.cs、Services/AuthService.cs、Services/ReviewService.cs、Utils/SmartTableExtractor.cs 等 50+ 个文件。
- **教训**：避免使用 PowerShell 的 `Set-Content` 批量修改代码文件，应使用专门的代码编辑工具或逐文件处理。

### 验证（日期类型与客户产品别整合）
- MSBuild 编译 0 错误。
- 日期字段改为 DateTime? 后，导入/导出/筛选/复制粘贴均正常工作。
- 客户表整合产品别后，管理窗口客户管理 Tab 显示客户名称/产品别/客户代码/备注四列。
- 主窗口启动时自动置前，子窗口独立不强制层级。
- 文件编码损坏问题已恢复，所有功能正常。

## 2026-08-19（下午）

### 修复

#### 39. 日期格式识别（"2026/8/18"与"8月7日"）
- **现象**：导入计划表/领退表时无法识别 "2026/8/18" 和 "8月7日" 类型的日期。
- **修改文件**：`Services/PlanExcelService.cs`、`Services/DatabaseService.cs`
- **修改内容**：
  - `ParseAnyDate` 重写：1）中文"M月d日"正则（年份从文件名推断，缺省当前年）；2）数字格式 TryParseExact（yyyy/M/d 等 7 种不变文化环境格式）；3）DateTime.TryParse 兼容回退。
  - 计划表导入开始/结束日期也传入文件名推断年份。

#### 40. 客户别与产品别分表管理（撤销上次错误合并）
- **需求更正**：客户别和产品别应分表管理——客户别 = Cust. Code 工作表 B、C 列（Cust. Code→ENDCUSTOMER），产品别 = G、H 列（Code→Product Type）。
- **修改文件**：`Models/AdminCatalogs.cs`、`Models/Review.cs`、`Services/AdminService.cs`、`Admin/Views/WindowAdmin.xaml(.cs)`
- **修改内容**：
  - 恢复 `Product` 实体（products 表：Name/Code/Remark）。
  - `Customer` 移除 Product 字段，保留 Name/Code/Remark。
  - `SyncCatalogsFromScheduleFile` 解析 Cust. Code：B/C 列→客户（Code/Name），G/H 列→产品别（Code/Name），Schedule 表机种映射同步补充两字典。
  - 管理窗口恢复"产品别管理"Tab（增删改查），客户管理 Tab 移除产品别列。

### 重构：计划表与领退表分表（双 Tab）

#### 41. 实体拆分
- **新增文件**：`Models/Requisition.cs`（requisitions 表）
- **修改文件**：`Models/Plan.cs`
- **修改内容**：
  - `Plan` 仅保留计划表字段（工作編號/产品别/客户别/机种名/阶段/测试项目/样品数/试验时间/负责人/开始结束日期/状况/备注）。
  - `Requisition` 承载领退表全部字段（領用日期/領料單据號/機種名稱/領出數量/S-N/D-C/REV/WorkOrder/回线RT工令/回线数量/線別/回线日期/入库字段/备注）。
  - 领退表不含测试项目列（按需求）。
  - 旧数据自动迁移：plans 表遗留领退字段一次性迁移到 requisitions 表（含旧日期文本清洗，无法解析的置空避免 FreeSql 读取崩溃）。

#### 42. 双表导入导出
- **修改文件**：`Services/PlanExcelService.cs`
- **修改内容**：
  - `ImportRequisition`：导入领退表到 requisitions 表，按 领料单据号→WorkOrder 匹配合并；OLE 对象提取保留。
  - `ImportSchedule`：导入计划表到 plans 表，按 工作編號 匹配合并。
  - `ExportRequisition`/`ExportSchedule`：分别从两表导出。
  - 新增 `GenerateReturnRtOrder`（RTAH{年月}{当月序号2位}）与 `GenerateJobNo`（{RT/QRT}{年月}{当月序号2位}）自动编号方法。

#### 43. 双 Tab 界面与操作
- **修改文件**：`Plans/ViewModels/PlansViewModel.cs`、`Plans/Views/WindowPlans.xaml(.cs)`
- **修改内容**：
  - 领退表/计划表分两个 Tab 展示，各自独立搜索、单元格编辑、右键菜单（编辑/删除/复制/粘贴/显示隐藏列）、列顺序与可见性持久化。
  - 计划表 Tab 保留条件筛选（机种/测试项目/产品别/客户别/负责人/阶段/状况/开始日期），领退表 Tab 关键字搜索。
  - 暂存提交/丢弃修改、变更日志覆盖两表。

### 新增：分表新增表单

#### 44. 领退表新增（WindowRequisitionEdit）
- **新增文件**：`Plans/Views/WindowRequisitionEdit.xaml(.cs)`（删除旧 `WindowPlanEdit`）
- **必填**：領用日期、領料單据號、機種名稱、領出數量、S/N（字符串或上传附件二选一）、REV.、Work Order。
- **自动补全**：D/C（WorkOrder 倒数第三位起两位）、線別（WorkOrder 倒数第六位起三位）、回线RT工令（可勾选自动生成 RTAH{年月}{编号}，也可手动输入）；计划表同步区（测试项目*/开始时间*/阶段* 必填；工作编号 RT{年月}{编号}、样品数=領出數量、产品别/客户别按机种映射、试验时间/负责人/结束日期按测试项目字典、状态默认 Ongoing）。
- 唯一键重复拦截（領料單据號/工作編號）。

#### 45. 计划表直接新增（WindowPlanDirectEdit）
- **新增文件**：`Plans/Views/WindowPlanDirectEdit.xaml(.cs)`
- **必填**：测试项目、开始时间、阶段、机种名、备注。
- **自动补全**：工作编号 QRT{年月}{编号}（非领用前缀）、产品别/客户别按机种映射、试验时间/负责人/结束日期按测试项目字典、状态默认 Ongoing、样品数可选填。
- 编辑模式沿用已有工作编号并校验格式。

#### 46. 提审支持领退表
- **修改文件**：`Services/ReviewService.cs`
- 新增 `SubmitRequisitionRequest` 与 `ApplyRequisitionChange`：普通用户新增/编辑领退记录走审核流程（Type="领退表单"），审核通过后写入 requisitions 表。

### 验证（分表）
- 端到端测试工具验证（真实文件）：计划表导入 286 条更新；"8月7日"格式日期解析 4 条（2026-08-07）；领退表导入 232 条、领用日期全部非空；requisitions 表无测试项目列；自动编号 RTAH260816/RT260817/QRT260803 正确递增；双表导出文件生成成功；主程序启动时旧数据自动迁移 226 条（日期清洗+按行解析）。
- MSBuild 编译 0 错误；临时测试工具与测试数据已清理。

### 改进：浏览/编辑分离、测试项目联动、清空入口迁移、右键编辑单元格

#### 47. 日期列浏览与编辑分离
- **修改文件**：`Plans/Views/WindowPlans.xaml`
- **修改内容**：5 个日期列（領用日期/回线日期/入库日期/开始日期/结束日期）浏览时用 TextBlock 显示简短字符串（`StringFormat={}{0:yyyy/M/d}`），进入单元格编辑时才切换为 DatePicker 选择框。

#### 48. 测试项目联动（表格内编辑）
- **现象**：表格内选择另一个测试项目后，负责人和试验时间仍是原有的，关联数据不联动。
- **修改文件**：`Plans/ViewModels/PlansViewModel.cs`、`Plans/Views/WindowPlans.xaml.cs`
- **修改内容**：新增 `AutoFillByTestItem`（按测试项目字典带出负责人/试验时间，并按开始日期+试验时间计算结束日期）；`Dg_Plans_CellEditEnding` 中测试项目列校验通过后调用联动，与机种联动一致。

#### 49. 右键单元格直接编辑
- **现象**：选择单个单元格右键时菜单无法进入编辑。
- **修改文件**：`Plans/Views/WindowPlans.xaml(.cs)`
- **修改内容**：右键菜单新增"编辑单元格"项；DataGrid 处理 `PreviewMouseRightButtonDown`，通过可视树定位鼠标所在单元格并设为 `CurrentCell`，菜单点击后 `BeginEdit()` 直接进入编辑模式（两表均支持）。

#### 50. 清空数据入口迁移至管理模块
- **修改文件**：`Admin/Views/WindowAdmin.xaml(.cs)`、`Plans/Views/WindowPlans.xaml`、`Plans/ViewModels/PlansViewModel.cs`
- **修改内容**：移除领退和计划窗口工具栏的"全部清空"按钮（VM 命令保留但禁用）；管理窗口顶部工具栏新增"清空计划数据"按钮（红色），仅管理员（admin.manage）可操作，需两次确认，调用 `PlanExcelService.ClearAll()` 清空领退表+计划表。

### 验证（浏览编辑分离）
- MSBuild 编译 0 错误；主程序启动冒烟测试正常（响应正常、数据库初始化无异常）。

## 2026-08-20

### 修复

#### 51. 工程文件重复引用
- **修改文件**：`ORT一键报告.csproj`
- **修改内容**：移除重复的 `<Compile Include="Models\Requisition.cs" />` 条目（保留原位置）。

#### 52. 编辑对话框直接写库 BUG（改走暂存）+ 机种联动 + 自动字段可编辑
- **现象**：编辑对话框点击保存后直接提交写库（未走暂存）；机种名称修改后产品别/客户别未更新；自动查询字段（产品别/客户别/负责人/试验时间/结束日期/D/C/線別）只读不可修改。
- **修改文件**：`Plans/Views/WindowRequisitionEdit.xaml(.cs)`、`Plans/Views/WindowPlanDirectEdit.xaml(.cs)`、`Plans/ViewModels/PlansViewModel.cs`
- **修改内容**：
  - 两个对话框不再直接写库：只构造 `RequisitionResult`/`PlanResult`，由 ViewModel 决定——普通用户走提审；技术员及以上暂存到内存集合（新增插入集合+待提交列表，编辑把结果字段复制回集合内对象触发属性通知），需点“提交保存”才写库。
  - 机种名称输入框绑定 `TextChanged`：修改机种名后自动带出产品别/客户别（仅填充空字段）。
  - 自动补全字段全部改为可编辑（产品别/客户别/负责人/试验时间/结束日期/D/C/線別/样品数），自动补全仅填充空字段不覆盖手改值。

#### 53. 产品类型导入不完全（代码映射表）
- **现象**：同一产品类型对应多个产品代码（如 Sever 对应 FS/PO/TC/SG 等多个 2 位代码）时，products 表按名称唯一只能保存一个代码，按代码查询其他机种失败。
- **修改文件**：`Models/AdminCatalogs.cs`、`Services/AdminService.cs`
- **修改内容**：
  - 新增 `code_mappings` 表（CodeType: C=客户别/P=产品别 + Code + Name，代码唯一），Cust. Code 表 B/C 列→客户代码映射、G/H 列→产品代码映射，支持同一名称多代码。
  - 代码规范化：纯数字代码补两位前导零（如 2→02）。

### 改进：产品别/客户别按机种名代码查询

#### 54. 机种名代码查询规则
- **规则**：客户别 = 机种名称第 8 位起的 2 位代码；产品别 = 机种名称开始的 2 位代码；代码→名称经 code_mappings 查询，映射缺失时回退机种映射表。
- **修改文件**：`Services/AdminService.cs`（`FindCustomerByModel`/`FindProductByModel`/`ModelToCustomerCode`/`ModelToProductCode`）、`Plans/ViewModels/PlansViewModel.cs`、两个编辑对话框。
- **实测**（真实文件）：代码映射同步客户代码 89 个/产品代码 24 个；抽查验证 28→DIEBOLD、24→DELL、4B→SUPERMICRO、EL→LENOVO、F9→AMZ、4C→JUNIPER、GQ→HPE、A2→CELESTICA 均正确；产品规则全部命中。

#### 55. EPPlus 许可统一到程序入口
- **修改文件**：`App.xaml.cs`、`Services/PlanExcelService.cs`、`Services/AdminService.cs`
- **修改内容**：`ExcelPackage.License.SetNonCommercialPersonal` 仅在 `App.OnStartup` 设置一次，移除各服务中的重复调用。

#### 56. 管理窗口改名与列调整
- **修改文件**：`Admin/Views/WindowAdmin.xaml(.cs)`
- **修改内容**：
  - “产品别管理”Tab 改名“产品类型管理”（对话框标题/提示同步）。
  - 客户管理列顺序：客户代码在前、客户名称在后；产品类型管理列顺序：产品代码在前、产品类型名称在后。
  - 默认按代码列排序（无代码排最后）。

#### 57. 表格样式与右键菜单
- **修改文件**：`Plans/Views/WindowPlans.xaml(.cs)`
- **修改内容**：
  - 单元格样式重构：CellStyle 去除内边距/边框，内容水平垂直居中；文本列 ElementStyle 居中、EditingElementStyle 无边框铺满单元格；日期列浏览文本也居中。
  - 右键菜单移除“编辑单元格”项。
  - 修复仅选择单个单元格时“编辑(对话框)”打不开：优先取右键定位的 `CurrentCell.Item` 所在行，不再依赖整行选中。

### 验证（代码查询规则）
- MSBuild 编译 0 错误；端到端测试工具验证代码映射同步与机种名查询规则（命中率 11/12，未命中项为 Cust. Code 表缺代码）；主程序启动正常；临时测试工具与数据已清理。

## 2026-08-26

### 修复

#### 58. 选中单元格变空白（领退和计划表）
- **现象**：选中单元格时内容消失（文字变白），取消选择后恢复。
- **根因**：FullCell 单元格样式固定白色背景，优先级高于默认模板的选中触发器；而选中时默认模板把前景设为白色 → 白底白字不可见。
- **修改文件**：`Plans/Views/WindowPlans.xaml`
- **修改内容**：FullCell 样式增加 IsSelected 触发器，选中时保持黑字白底。

#### 59. SN 栏支持回车换行与高度自适应（领退表表单）
- **现象**：SN 输入框无法输入回车（回车会移动焦点）。
- **修改文件**：`Plans/Views/WindowRequisitionEdit.xaml`
- **修改内容**：SN 输入框设为多行（AcceptsReturn/AcceptsTab + TextWrapping=Wrap + MinLines=2/MaxLines=8），高度随行数自动伸缩，超过 8 行显示滚动条。

#### 60. 领退表编辑时带入计划表对应记录（含机种）
- **现象**：编辑领退记录时，对话框计划同步区为空，未载入对应计划记录。
- **修改文件**：`Plans/Views/WindowRequisitionEdit.xaml(.cs)`、`Plans/ViewModels/PlansViewModel.cs`
- **修改内容**：
  - 编辑模式下按 备注含回线RT工令 → 备注含 WorkOrder → 机种+领用日期同开始日期 三级规则查找关联计划，载入计划同步区（测试项目/阶段/开始时间/工作编号/样品数/产品别/客户别/负责人/试验时间/结束日期，含机种）。
  - 保存时构造关联计划的修改结果，由 ViewModel 暂存（技术员）或提审（普通用户），与领退记录一同提交。
  - 编辑模式下测试项目联动（负责人/试验时间/结束日期）也生效，仅填充空字段。

### 新增：设置页面（VSCode 风格，JSON 保存）

#### 61. 设置服务与模型（JSON）
- **新增文件**：`Models/AppSettings.cs`、`Services/AppSettingsService.cs`
- **修改内容**：
  - 设置以 JSON 保存到 `Data\settings.json`（Newtonsoft 序列化），损坏时回退默认值。
  - 设置项：UI（字体族/字号）；路径（计划表/领用表/报告/ATE数据/EMI数据 默认目录）。
  - 提供 `ApplyFont`/`ApplyFontToAll`（字体实时应用到所有窗口）与各路径目录查询（目录无效时返回 null）；保存后触发 SettingsChanged。

#### 62. 设置页面（左树 + 右详情 + 同步滚动）
- **新增文件**：`Main/Views/WindowAppSettings.xaml(.cs)`
- **修改内容**：参考 VSCode 设置页：左侧树状目录（UI/字体；路径/五个路径项）与右侧设置详情双向同步滚动，点击树节点跳转到对应设置节；右侧滚动时树自动高亮当前节。字体族（系统字体列表）/字号/各路径（文本框+浏览按钮）修改即保存并实时生效。

#### 63. 入口与设置接入
- **修改文件**：`MainWindow.xaml(.cs)`、`App.xaml.cs`、`Admin/Views/WindowAdmin.xaml.cs`、`Reports/ViewModels/MainReportViewModel.cs`、`Reports/ViewModels/BaseReportPageViewModel.cs`、`Reports/ViewModels/EMIReportViewModel.cs`、`ORT一键报告.csproj`
- **修改内容**：
  - 主窗口菜单栏新增"设置"入口；启动时应用设置字体，设置变更实时刷新。
  - DI 注册 `AppSettingsService`（单例）。
  - 路径接入：导入计划表/领用表用设置目录；一键报告选报告概览用报告路径；选择 ATE 数据用 ATE 路径；选择 EMI 数据用 EMI 路径。
  - csproj 注册新文件（模型/服务/设置窗口 XAML+代码）。

### 验证（设置页面）
- MSBuild 编译 0 错误；主程序启动冒烟测试正常（响应正常、数据库与设置服务初始化无异常）。
