# ORT实验室管理系统 更新日志

> 记录每一次版本更新：标题为 `## YYYY-MM-DD（vX.Y.Z）`，用户未声明版本号时省略括号。按时间正序追加，最新一节在文件末尾。
> 历史逐文件变更记录见 `修改记录.md`（保持只读，不再追加）。

## 2026-09-21

- **新增「更新日志」机制（本文件）**：在根目录建立 `更新日志.md`，此后每次版本更新在这里追加一节；`修改记录.md` 保持只读，不再写入。
- **新增技能 `update-log`**（`.dsh/skills/update-log/SKILL.md`）：每轮对话有文件改动就把改动总结成「更新内容」；当用户声明这是一次版本更新，或该轮改动大到足以当作一次更新（跨多模块、含面向用户的新功能或行为变化等，满足判定标准中 ≥2 项）时，在本文件追加一节更新日志。
- **发布规则**：正序追加、最新一节在文件末尾、同日并入末尾小节、不改写历史条目；只在用户声明版本更新时才在标题写版本号（来源为用户口述或 `Properties/AssemblyInfo.cs` 的 `AssemblyFileVersion`），不自行自增版本；更新内容只写真实改动，不写过程信息与空话。
- **收尾顺序**：先写更新日志，再走 `git-commit-sync`（add → commit → push），让日志进入本轮同一个 commit；同一套规则写进 `AGENTS.md` 的「每轮对话收尾」。
