# ORT实验室管理系统 更新日志

> 只记录**程序本身**的更新：新功能、行为/界面变化、缺陷修复、数据与设置格式变化、破坏性变化。
> 标题格式 `## YYYY-MM-DD（vX.Y.Z）`，用户未声明版本号时省略括号；按时间正序追加，最新一节在文件末尾。
> 技能、`AGENTS.md` 约定、构建与验证脚本等仓库协作类改动不进本文件（历史变更记录见 `修改记录.md`）。

## 2026-09-21

- **领用和计划（搜索栏）**：搜索框右侧原来的提示文字换成「进行中 / 预排测试 / 已结案」三个彩色徽标，数字按当前搜索与列筛选后**可见的记录**统计，随搜索、表头筛选与单元格编辑实时刷新（`Plans/ViewModels/PlansViewModel.cs`）。
- **计划表（状况列）**：状况列按值显示底色——进行中黄、预排测试红、已结案绿；三套内置主题各配一套（深色方案用暗底亮字），`Close`/`Closed`/结案都算已结案、认不出的写法不上色，选中的单元格仍显示选中色（`Models/Plan.cs`、`Utils/PlanValidation.cs`、`Themes/FluentTheme.xaml` 等）。
- **修复**：点「领用和计划」报 `InvalidOperationException：无法对只读属性 OngoingCount 进行 TwoWay 或 OneWayToSource 绑定`，窗口打不开。原因是 `Run.Text` 的绑定默认就是 TwoWay，而统计数字只有私有 setter；三个统计绑定改为显式 `Mode=OneWay`（同窗口原有的 SN 文件列绑定本来就是 OneWay）。

