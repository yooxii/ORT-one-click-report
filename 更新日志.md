# ORT实验室管理系统 更新日志

> 只记录**程序本身**的更新：新功能、行为/界面变化、缺陷修复、数据与设置格式变化、破坏性变化。
> 标题格式 `## YYYY-MM-DD（vX.Y.Z）`，用户未声明版本号时省略括号；按时间正序追加，最新一节在文件末尾。
> 技能、`AGENTS.md` 约定、构建与验证脚本等仓库协作类改动不进本文件（历史变更记录见 `修改记录.md`）。

## 2026-09-21

- **领用和计划（搜索栏）**：搜索框右侧原来的提示文字换成「进行中 / 预排测试 / 已结案」三个彩色徽标，数字按当前搜索与列筛选后**可见的记录**统计，随搜索、表头筛选与单元格编辑实时刷新（`Plans/ViewModels/PlansViewModel.cs`）。
- **计划表（状况列）**：状况列按值显示底色——进行中黄、预排测试红、已结案绿；三套内置主题各配一套（深色方案用暗底亮字），`Close`/`Closed`/结案都算已结案、认不出的写法不上色，选中的单元格仍显示选中色（`Models/Plan.cs`、`Utils/PlanValidation.cs`、`Themes/FluentTheme.xaml` 等）。
- **修复**：点「领用和计划」报 `InvalidOperationException：无法对只读属性 OngoingCount 进行 TwoWay 或 OneWayToSource 绑定`，窗口打不开。原因是 `Run.Text` 的绑定默认就是 TwoWay，而统计数字只有私有 setter；三个统计绑定改为显式 `Mode=OneWay`（同窗口原有的 SN 文件列绑定本来就是 OneWay）。
- **工作编号可手动修改**：计划表新增/编辑（`QRT…`）与领退表新增/编辑（`RT…`）的「工作编号」不再是只读——不填时仍按日期自动生成，填了就用手填的（自动补全不会覆盖），保存时照旧校验格式与重号（`Plans/Views/WindowPlanDirectEdit.xaml(.cs)`、`WindowRequisitionEdit.xaml(.cs)`）。
- **新增「转为领用」入口**：计划表新增窗口在「保存」右边多一个「转为领用」，把当前填好的机种/测试项目/开始日期/数量/负责人等直接带进「领退表新增」窗口，补完领料信息保存后即按领用流程建立 `RT` 计划（只在新增时出现，编辑模式隐藏）。
- **新增必填提示**：计划表新增/编辑、领退表新增/编辑、测试项目新增/编辑的按钮行左侧显示「带 * 为必填项」，与标签上原有的 `*` 标记对应。
- **修复**：这两个窗口的「保存/取消」按钮文字被压掉——按钮写死了 `Height="28"`，而主题按钮模板自带 `Padding="8,7"`，内容放不下；去掉写死高度让按钮自适应（实测高度 28 → 31.2）。

## 2026-09-22

- **编号跟随日期刷新**：领退表/计划表新增时，把日期填错了再改回来，原先自动生成的「工作编号」不再停在旧日期上——只要编号还是自动生成的那个（没手动改过）就跟着新日期刷新，手动填过的保持不动；另外点「工作编号」标签可以随时按当前日期重新生成（`Plans/Views/WindowRequisitionEdit.xaml(.cs)`、`WindowPlanDirectEdit.xaml(.cs)`）。
- **回线RT工令改为可选**：领退表的「回线RT工令」取消原来的自动生成，改为由「需要回线」勾选框控制——不勾选时留空并禁用输入，表示这笔领用**无需回线**；勾选后可手动填写，点「回线RT工令」标签按领用日期生成 `RTAH{年月}{编号}`，勾了却没填保存时会提示；该字段在库中允许为空。
- **计划表同步信息改为可选（折叠区域）**：领退表新增窗口的「计划表同步信息」收进可折叠区域，默认**折叠 = 不选**，只登记领退记录、不再顺带建立计划表记录；展开表示要同步，下列字段（测试项目/阶段/工作编号等）成为必填并同步建立计划表记录。从计划表「转为领用」进来的会自动展开；编辑时若这条领退找不到关联计划，展开后保存会给出说明（`Plans/ViewModels/PlansViewModel.cs` 同步处理计划结果为空的情况）。
- **自定义序列号重复检查**：领退表选「自定义输入」时，点保存会按「每行一个序列号」检查重复——既查本次输入内部的重号，也查领退表里已有的序列号（编辑时排除本记录本身）；有重复会列出具体序列号与来源，并问「是否重新输入」（选「是」返回修改，选「否」继续保存）。
- **领退表右键菜单新增「回线 / 入库」**（只加在领退表右键菜单，计划表右键菜单没有）：
  - **回线**：打开回线登记窗口，只读展示机种名称、回线RT工令、回线数量、线别（都可选中复制，Ctrl+C），下面一个默认当天的回线日期；点「回线」把该日期写入这条领退记录的回线日期。
  - **入库**：打开入库登记窗口，只读展示机种名称、领料单据号、序列号、工令、线别（都可选中复制），再加「入库单号 / 入库数量 / 入库日期」三个输入框；点「入库」把这三个值写入这条领退记录。
  - 两者都只改当前表格里的记录（暂存），点「提交保存」时才写库并记变更日志；没有表格编辑权限时给出提示（`Plans/Views/WindowPlans.xaml(.cs)`、新增 `WindowRequisitionReturn`、`WindowRequisitionStockIn`）。
- **修复**：领退表编辑窗口里修改「工作编号」原先不生效（编辑分支没有把这个字段写回关联计划），现在会连同格式与重号校验一起保存。
- **表头筛选改为按共同前缀分组**：计划表/领退表的列筛选不再一次性把所有取值铺开——开头相同的取值收进一组，标题显示共同前缀与数量（如 `RT260…（234）`），**默认折叠**、可逐层展开；组内第一项「全选本组（N 项）」一次即可勾选或取消整组。取值多时仍提供搜索框：搜索时隐藏分组树、改为平铺列出匹配项（`Plans/Views/WindowPlans.xaml.cs`）。
- **空单元格可以作为筛选项**：单元格为空/null（含只有空格/制表符的）时，筛选列表最前会多出一项「（空白）」，可以只筛出空值行、也可以把它排除掉；此前这类单元格完全不参与筛选，列筛选一启用就会连空值行一起被隐藏（`Plans/ViewModels/PlansViewModel.cs`）。

