<Window x:Class="ORT一键报告.Review.Views.WindowReview" Background="{DynamicResource WindowBackgroundBrush}" Foreground="{DynamicResource TextPrimaryBrush}"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        mc:Ignorable="d"
        Title="{lex:Loc Review_Title}" Height="600" Width="1000" MinHeight="420" MinWidth="760"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
            <Label Content="{lex:Loc Admin_Status}" VerticalContentAlignment="Center"/>
            <ComboBox x:Name="cb_status" Width="110" VerticalContentAlignment="Center" SelectionChanged="Cb_Status_SelectionChanged">
                <ComboBoxItem Content="{lex:Loc Common_All}" Tag="" IsSelected="True"/>
                <ComboBoxItem Content="{lex:Loc Review_Pending}" Tag="待审核"/>
                <ComboBoxItem Content="{lex:Loc Review_Approved}" Tag="已通过"/>
                <ComboBoxItem Content="{lex:Loc Review_Rejected}" Tag="已驳回"/>
            </ComboBox>
            <Button Content="{lex:Loc Common_Refresh}" Padding="10,3" Margin="15,0,0,0" Click="Btn_Refresh_Click"/>
            <Button Content="{lex:Loc Review_OpenDetail}" Padding="10,3" Margin="15,0,0,0" Click="Btn_Detail_Click"
                    ToolTip="{lex:Loc Review_ToolTip_OpenDetail}"/>
        </StackPanel>

        <!--请求列表（工作流主界面）-->
        <DataGrid x:Name="dg_requests" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" CanUserSortColumns="True"
                  SelectionMode="Single" SelectionUnit="FullRow"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                  MouseDoubleClick="Dg_Requests_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="45"/>
                <DataGridTextColumn Header="{lex:Loc Review_Type}" Binding="{Binding Type}" Width="70"/>
                <DataGridTextColumn Header="{lex:Loc Review_Action}" Binding="{Binding Action}" Width="55"/>
                <DataGridTextColumn Header="{lex:Loc Review_Summary}" Binding="{Binding Summary}" Width="*"/>
                <DataGridTextColumn Header="{lex:Loc Review_Requester}" Binding="{Binding RequesterName}" Width="80"/>
                <DataGridTextColumn Header="{lex:Loc Admin_Status}" Binding="{Binding Status}" Width="65"/>
                <DataGridTextColumn Header="{lex:Loc Review_RequestTime}" Binding="{Binding CreatedAt, StringFormat=yyyy-MM-dd HH:mm}" Width="120"/>
                <DataGridTextColumn Header="{lex:Loc Review_Reviewer}" Binding="{Binding ReviewerName}" Width="80"/>
                <DataGridTextColumn Header="{lex:Loc Review_ReviewTime}" Binding="{Binding ReviewedAt, StringFormat=yyyy-MM-dd HH:mm}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <StatusBar Grid.Row="2">
            <StatusBarItem x:Name="status_msg" Content="{lex:Loc Review_DoubleClickHint}"/>
        </StatusBar>
    </Grid>
</Window>
