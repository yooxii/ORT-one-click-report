<Window x:Class="ORT一键报告.Review.Views.WindowReview"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="审核" Height="600" Width="1000" MinHeight="420" MinWidth="760"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
            <Label Content="状态" VerticalContentAlignment="Center"/>
            <ComboBox x:Name="cb_status" Width="110" VerticalContentAlignment="Center" SelectionChanged="Cb_Status_SelectionChanged">
                <ComboBoxItem Content="全部" IsSelected="True"/>
                <ComboBoxItem Content="待审核"/>
                <ComboBoxItem Content="已通过"/>
                <ComboBoxItem Content="已驳回"/>
            </ComboBox>
            <Button Content="刷新" Padding="10,3" Margin="15,0,0,0" Click="Btn_Refresh_Click"/>
            <Button Content="打开详情" Padding="10,3" Margin="15,0,0,0" Click="Btn_Detail_Click"
                    ToolTip="查看选中请求的详情并审核"/>
        </StackPanel>

        <!--请求列表（工作流主界面）-->
        <DataGrid x:Name="dg_requests" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" CanUserSortColumns="True"
                  SelectionMode="Single" SelectionUnit="FullRow"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                  MouseDoubleClick="Dg_Requests_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="45"/>
                <DataGridTextColumn Header="类型" Binding="{Binding Type}" Width="70"/>
                <DataGridTextColumn Header="操作" Binding="{Binding Action}" Width="55"/>
                <DataGridTextColumn Header="摘要" Binding="{Binding Summary}" Width="*"/>
                <DataGridTextColumn Header="请求人" Binding="{Binding RequesterName}" Width="80"/>
                <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="65"/>
                <DataGridTextColumn Header="请求时间" Binding="{Binding CreatedAt, StringFormat=yyyy-MM-dd HH:mm}" Width="120"/>
                <DataGridTextColumn Header="审核人" Binding="{Binding ReviewerName}" Width="80"/>
                <DataGridTextColumn Header="审核时间" Binding="{Binding ReviewedAt, StringFormat=yyyy-MM-dd HH:mm}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <StatusBar Grid.Row="2">
            <StatusBarItem x:Name="status_msg" Content="双击请求行打开详情"/>
        </StatusBar>
    </Grid>
</Window>
