<UserControl x:Class="ORT一键报告.Views.SettingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ORT一键报告.Views"
             xmlns:vm="clr-namespace:ORT一键报告.ViewModels"
             xmlns:conv="clr-namespace:ORT一键报告.Converters"
             d:DataContext="{d:DesignInstance Type=vm:SettingItemViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="300">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="15">
        <StackPanel Margin="0,10,0,0">
            <TextBlock Text="{Binding Label}" FontSize="16" FontWeight="Bold" Margin="0,0,0,5" Foreground="#007ACC"/>
            <Border BorderBrush="#EEEEEE" BorderThickness="1" Padding="10,5" CornerRadius="4">
                <StackPanel x:Name="contentControl">
                    <ItemsControl ItemsSource="{Binding}">
                        <ItemsControl.ItemTemplateSelector>
                            <conv:SettingTemplateSelector 
                                    StringTemplate="{StaticResource StringTemplate}"
                                    BoolTemplate="{StaticResource BoolTemplate}"
                                    ComboTemplate="{StaticResource ComboTemplate}"/>
                        </ItemsControl.ItemTemplateSelector>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
